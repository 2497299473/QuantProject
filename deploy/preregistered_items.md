# QuantV1 预注册待办登记表

> 用途：登记「已确认要做、但**尚未实现**」的预注册事项。登记 ≠ 实现；实现前不得静默改变口径。
> 创建：2026-09-11（Summer 要求「只登记、不实现」）。存放在 `deploy/` 的理由与 `daily_summary_task.md` 一致：本目录 git 内可追溯。
>
> **登记纪律**
> - 只增不改历史条目；条目状态变化时追加修订行，不覆盖原文。
> - 每条须含：触发条件 / 受影响范围 / 触发时必须做的动作 / 已知约束 / 证据 / 明确不做。
> - 任何实现都必须走完整流程：**先把判据写死（预注册）→ 再跑回测 → 再裁决**，不得临场改规则。
> - 本表不构成投资建议。

---

## PR-20260911-01 · 回测窗口跨 2018-06-26 ~ 2019-07-17 时必须处理 002112 分红除息

- **登记日期**：2026-09-11
- **状态**：🟡 **已登记 / 未实现**（截至登记日：未改任何生产 `.py`、未改口径、未换数据源）
- **紧迫度**：低（当前所有在跑结论不受影响，见「不受影响范围」）

### 触发条件

任何以**单位净值日收益**为输入的因子或回测，其滚动窗口或样本区间落在 **2018-06-26 ~ 2019-07-17（CST）** 之间时，本条生效。

### 受影响范围

- **仅 002112**。池内另 3 只（025687 / 022853 / 002207）已实施分红为 **0**（各 1 条记录但除息日为空），实测近 5 年单位 vs 复权日收益分歧 **0 / 1215、0 / 174、0 / 415**。
- **不受影响范围（重要，勿扩大化）**：002112 近 5 年 1216 条单位/复权日收益分歧 **0 条** → 现有 60 日 OOS、2021-2023 衰减分析、v7 基线**均不含污染日**，无需回溯修正。

### 事实底数（002112，共 5 次分红）

| 除息日 | 除息前 → 除息日单位净值 | 表观涨跌 | 每份派现 | 其中派现占比 | 剔除派现后 |
|---|---|---|---|---|---|
| 2018-06-26 | 1.1275 → 1.0771 | **-4.47%** | 0.0480 | 4.26pp | -0.21% |
| 2018-09-26 | 1.0634 → 1.0264 | **-3.48%** | 0.0400 | 3.76pp | +0.28% |
| 2018-12-25 | 1.0117 → 1.0010 | -1.06% | 0.0100 | 0.99pp | -0.07% |
| 2019-05-31 | 1.0418 → 1.0316 | -0.98% | 0.0100 | 0.96pp | -0.02% |
| 2019-07-17 | 1.0474 → 1.0074 | **-3.82%** | 0.0400 | 3.82pp | +0.00% |

- 派现占比与表观跌幅几乎完全吻合 → 这些「下跌」是分红除息，**不是市场损失**。
- 合计派现 **0.148 元/份**；同花顺复权/单位 = 5.9387/5.1610 = 1.1507（+13.1%），与派现累计幅度同量级 → 数据自洽。
- 生产口径：`core/data_loader.py` 解析东财 pingzhongdata `Data_netWorthTrend`（**单位净值**，未复权）；`lsjz` 的 `acc_nav` 仅用于取申赎状态，未参与任何计算。

### 触发时必须做的动作（实现时须预先写死判据，三选一）

1. **改用复权净值**（口径最干净，但等于更换计算输入，须重跑并标注「口径变更日」）。
2. **按派现加回**：除息日 `r = (nav_t + 每份派现) / nav_{t-1} - 1`（改动最小，仅影响 5 个日收益点）。
3. **窗口剔除该 5 日**（最省事，但丢样本，须在报告中显式注明）。

> 三者利弊与选择应在实现当次的预注册文中写死，不得跑完再挑。

### 已知约束（实现前必须解决）

- 同花顺 `performance/nav` 的 `range` 上限为 `fyear`（≈5 自然年，实测最早 2021-09-10）→ **无法从同花顺获取 2018-2019 的复权净值**。若选方案 1，须另找复权源或按本条派现表自算。
- 本地单位净值序列**不含分红标记**，无法自行发现除息日 —— 除息日与每份派现金额来自同花顺 `corporate-actions/dividends`。
- 未审计其它公司行为（份额折算 / 拆分等）；若日后需要，同法扩展。

### 证据与复现

- 完整诊断报告：`output/nav_dividend_audit_20260911.md`（只读，未改代码）
- Obsidian 笔记：`量化交易工具\盘中估值验证与同花顺源评估-20260911.md` §六
- 复现脚本：`OpenSquilla .fetch/audit_all_pool.py`（池内 4 只全扫）、`check_exdiv.py`（除息日本地核对）、`probe_div_raw.py`（除息日原文）、`adj_vs_unit.py`（单位 vs 复权）

### 明确不做（登记时的边界）

- ❌ 不替换主数据源（东财仍为净值主源）。
- ❌ 不改变现有 10 维打分输入口径。
- ❌ 不改任何生产 `.py` / `config.json` / 定时任务表。
- ❌ 不因本条而回溯修正任何已发布结论（近 5 年无污染日）。

### 待 Summer 决定（可选）

是否把本条接入 `daily_summary_task.md` 的「待拍板清单」过滤条件，使其在窗口临近时自动浮出。该改动会修改自动化任务行为，**未执行**，留待 Summer 拍板。

---

### 修订记录（追加，不覆盖原文）

**2026-09-11 13:22 · 上方「待 Summer 决定」已拍板并执行**

- Summer 选定 B + C：接入自动浮出 + 提交入库。
- 已修改 `deploy/daily_summary_task.md`：第四步「入列标准」新增末条（预注册登记表触发）+ 新增「**预注册浮出细则**」段 + 额度段补排序与不得静默丢弃规则。
- **接入方式的边界**（保留原提请时承诺的克制）：
  - 日汇总任务对本表**只读**，不得改写状态；🟡→🔴 推进只能由 Summer 人工执行。
  - 触发判断**只允许复用当天已收集的运行证据**（daily_runs 小节 / shadow 末行 / 新报告文件名），**禁止为浮出触发而新增任何数据请求或运行 QuantV1 脚本** —— 避免为一条低紧迫度待办去撞东财频控（AGENTS.md 铁律第 1 条）。
  - 预注册项与其它待拍板项共用「每天最多 3 条」额度，但不得被静默丢弃（最多降入「其他」行）。
- 本条状态**仍为 🟡 已登记 / 未实现**：接入浮出机制 ≠ 实现分红处理，生产净值口径未变。

**2026-09-14 09:3x · 三选一已拍板：方案 1（改用复权净值）——随 D-full 启动，挂 09-26 节点**

- Summer 选定**方案 1**。触发前提已确认：仅当基金史前延至 2019-07-17 之前（= D-full）才生效；
  本窗口 09-16~18 跑 D-lite，**本条继续不触发**，实施排在 **09-26** 季度重估节点。
- ⚠️ **方案 1 的两个前置（开跑前必须验，任一不成立走预注册失败路径，不得临场改选方案）**：
  - **G1 复权源**：本条 §已知约束 L53 已写死同花顺拿不到 2018-2019 复权净值（`fyear` ≈ 5 年上限）。
    → 方案 1 只能走 (a) 东财 `lsjz` 分页抓 `acc_nav` 自算，或 (b) 用本条 §事实底数 5 次派现表自算含权序列。
  - **G2 本地缓存实测（本次新发现，推翻预注册 §四 原注）**：`data/klines/*.json` 的 `lsjz.records`
    **每只仅 20 条**（2026-08-14→09-10），因 `config.json:120` 硬编码 `pageSize=20`
    → 污染窗 `acc_nav` 本地覆盖 = **0 行**，「已在本地缓存」不成立。
  - **∴ 默认按 (b) 派现表自算执行**（零网络、5 个除息点）；(a) 需 ~132 请求/基金、撞 IP 频控，
    **仅在 (b) 覆盖不足时、且在 09-26 专用受控窗口内执行**。
- 本条状态**仍为 🟡 已登记 / 未实现**：选定方案 ≠ 已实施；生产净值口径本日未变，未改任何 `.py` / `config.json`。

---

## PR-20260913-01 · Shadow 降级授权到期（2026-10-12）必须复核，禁止静默续期

- **登记日期**：2026-09-13
- **状态**：🟡 **已登记 / 未实现**（触发时点在未来；登记 ≠ 实现）
- **紧迫度**：中（到期日 2026-10-12；自动浮出机制会在临近时提请）

### 背景（口径来源）

Summer 2026-09-13 16:59 拍板 D2「预注册降级口径」：因晋升层「三周期 CI 下界全 > 0」
在 n≈900 / 306 日块下对 T+1/T+3 **结构性不可达**（需 |IC|≳0.065，现值 0.005/0.002；
证据见 `output/forecast_lab_mde_20260912.md` + `output/forecast_lab_review_v2_20260912.md` §2.2），
`shadow_policy` 每交易日拒记 → 前瞻证据链断裂。

已落地的旁路：`data/promotion_prereg.json`（判据 `prereg_shadow_v1`，expiry=2026-10-12）
+ `core/model_registry.evaluate_prereg_degradation()` + `shadow_policy.resolve_promotion_mode()`。
**作用域硬边界**：仅授予纸面记录资格；`verify_approval()`（对外展示门）不读取旁路、行为不变。

### 触发条件

任一即触发本条：
1. 系统日期 ≥ **2026-10-05**（到期前 1 周的预备窗口）；
2. 2026-09-26 季度证据重估产出新结论；
3. `shadow_policy` 出现 `prereg_expired` 拒记（说明已过 10-12 仍未复核）。

### 触发时必须做的动作（三选一，先写死再选）

1. **续期**：若降级授权期间的前瞻 shadow 样本已积累出可用证据（≥20 交易日）
   → 以新证据重判是否仍需降级，重新登记 expiry（须附当次复核报告哈希）。
2. **废止**：若晋升判据已按新规则（如 D1-c 双端点、或候选 D 扩样本后的可达门槛）重建
   → 设 `enabled: false` 或删除本文件，Shadow 回到「完整批准才记录」。
3. **转正**：若 T+1/T+3 在前瞻样本上真的显著 → 走 `derive_promotion` 主路径正常晋升，
   降级旁路立即废止（旁路不得成为长期常驻通道）。

### 已知约束

- **禁止静默自动续期**：任何续期都必须是一次显式拍板 + 文件修订留痕（只增不改）。
- 到期未复核 = 自动阻断（代码 D6 已如此实现，不会悄悄放行）；届时 22:30 Shadow
  会拒记并在日汇总「待拍板」浮出，属于**预期的坏消息**，不是故障。
- 两类证据永不混池：降级记录逐条带 `contract.promotion_mode = "prereg_degraded"`，
  筛选/统计时必须与 `approved_full` 分开，否则晋升证据被污染。

### 证据与复现

- 规则预注册（先于实现落盘）：`output/forecast_lab_prereg_rules_20260913.md` §二 R2
- 回归测例：`tests/test_model_registry.py::TestPreregDegradation`（10 条，含过期边界
  10-12 有效 / 10-13 阻断、sha 不符拒、T+5 不显著拒、T+1 负 IC 拒）
  + `tests/test_shadow_policy.py::TestPromotionGate`（3 条，含「完整批准时不得读旁路」
  短路断言）+ `::TestFrozenSampleSource`（2 条，零网络不回退）
- 隔离性验证：`verify_approval()` 在降级授权生效时仍返回
  `(False, 'validation_not_approved')`（已由 happy-path 测例断言）

### 明确不做

- ❌ 不改 `derive_promotion` v1「三周期全过」（主授权口径保持原样，可回滚）。
- ❌ 不改 `config.forecast.model_ready`（维持 false）、不改 registry 任何字段。
- ❌ 不把降级授权扩大到对外展示、决策引擎、或任何下单链路。

---

## PR-20260914-01 · 候选 D 研究池面板：预注册已落盘，窗口开跑前须收齐 §九 勾选

- **登记日期**：2026-09-14
- **状态**：🟡 **已登记 / 未实现**（预注册文已落盘；实现未开始。登记 ≠ 实现）
- **紧迫度**：高（09-16~18 裁决窗口主任务；Summer 2026-09-14 00:12 拍板「先给 D」，候选 C 后置）

### 触发条件

今天 ≥ 2026-09-16，或 Summer 明确指令提前开跑。

### 触发时必须做的动作

1. **开跑前置**：先收齐预注册文 §九 三项勾选——① 范围 D-lite / D-full；② 09-18 候选槽 A″ / C′；
   ③ 确认 T3 不达标即停、不临场放宽。**未齐 → 只提醒，不开跑。**
2. 执行顺序（§五~§七）：受控一次批量刷新 17 序列 + 4 基金 → 面板冻结与 K 线指纹**同批产出**
   → T1（两次生成同 sha256）→ T2（与 0910 冻结逐值一致）→ T3（面板配对 MDE80 ≤ 0.05）→ 才开候选槽。
3. 任一硬判据不过 → 如实报告 + 停，禁止放宽阈值（判据先写死的铁律）。

### 已知约束

- 面板特征空间 = `features_a158lite` 50 特征（proxy 无持仓可穿透，现 7 特征不可算，修订节 R1）；
  与旧 4 只池（7 特征）的数字**只作方向参照，禁止横比**。
- relaxed 代理（电力/煤炭/农业）分层报告、不混池（`data/basket_20260902.json` 原纪律）。
- PR-20260911-01 在 D-lite（基金史自 2020-04-27）下**不触发**——分红污染窗 2018-06-26~2019-07-17
  无基金行（依据见预注册 §四）；本条**不改变** PR-20260911-01 的 🟡 状态；D-full 前必须先三选一。
- 降级/频控征兆 → 立即停手不重试（继承 `freeze_samples.py` RATE_MARKERS 纪律，退出码 1）。

### 证据与复现

- 预注册文：`output/forecast_lab_prereg_D_panel_20260914.md`（含修订节 R1：A″ 特征空间澄清）
- 底档：`data/basket_20260902.json`（代理表 + relaxed 纪律）、`output/basket_etf_probe_20260902.md`（探测）、
  `output/forecast_lab_mde_20260912.md`（宽度≈2 / MDE=0.117 实测）、09-14 凌晨缓存实测
  （13 主代理全在 `data/stock_klines` / `data/sector_klines` → 零新增数据采购）。

### 明确不做

- ❌ 不改 config / model_registry / registry.json / 任何生产 `.py` / 定时任务（面板件只进 `forecast_outputs/`）。
- ❌ 不动 D1-c 门槛与 Shadow 降级授权（与 PR-20260913-01 解耦，10-12 到期复核照旧）。
- ❌ 不在 T3 达标前跑任何候选结论。

### 修订记录（追加，不覆盖原文）

**2026-09-14 09:3x · §九 三项已收齐 → 状态改为 🟢 已拍板 / 未实现；第③项条款被替换**

Summer 原话：「D-full / C′ regime_cond / T3 功效不达标的话临场放宽」。逐项处理：

| 项 | Summer 选定 | 与推荐项 | 落盘方式 |
|---|---|---|---|
| ① 范围 | **D-full** | 非推荐（推荐 D-lite） | 时间归属按 §八：不占本窗口，挂 09-26；**本窗口实跑 D-lite** |
| ② 候选槽 | **C′ regime_cond** | 非推荐（推荐 A″） | 直接采用；R1 关于 A″ 的本窗口不生效 |
| ③ T3 | 原话“**临场放宽**” | — | **未照录**；改为**事前预注册失败路径**（R2 之二） |

- **第③项为何未照录**：“临场放宽”与本条动作 #3「任一硬判据不过 → 如实报告 + 停，禁止放宽阈值」
  及登记纪律「不得跑完再挑」正面冲突。**合规替代 = 跑之前把失败路径写死**：阈值 0.05 不动，
  但「不过时走哪条」预注册（停候选槽 → 报缺口 → 自动登记 PR-20260914-02 → 剩余预算转 T1/T2 留档）。
  连续 2 次不过 → 挂 09-26 与 D-full 并案。**若 Summer 要的是“跑完看到数字再放宽”，那仍是本纪律禁止的，
  需另起新预注册文单独拍板。**
- 动作 #1 已满足（三项齐）。动作 #2 的执行顺序不变，但 **面板内容 = D-lite**（基金史 2020-04-27 起）。
- 动作 #3 被 R2 之二替代（阈值未动，仅补失败后续作）。
- 本条状态：**🟢 已拍板 / 未实现**。未改任何生产 `.py` / `config.json` / 定时任务；面板件只进 `forecast_outputs/`。
- ⚠️ 新增开跑前硬约束（见预注册文 R2 之三）：`output/*.md` 全类被 `.gitignore` 忽略
  → **预注册文不在 git 历史**，“判据先写死”无法用提交历史自证。收编与否待 Summer 拍板，本窗口未动。

**2026-09-15 00:1x · R2 之三收编闭环 + 09-16 受控批量 11:20 硬停线（Summer 09-14 深夜拍板「三条都按建议执行」的落地记录）**

- 上方修订「收编与否待 Summer 拍板」→ 已于 09-14 提交 `35176c7` 收编（.gitignore 例外 + 5 份预注册文入库），预注册文侧以 R3 闭环登记；本表只追加不改原文。
- §已知约束新增一条（操作性时间盒，非判据）：09-16 批量刷新 **09:30 晨检后开跑、11:20 起零东财新请求**；11:20 未完成 = 停手不重试，缺口顺延下一交易日 09:30 后受控补刷（详见预注册文 R3 之二/之三）。
- 本条状态维持 **🟢 已拍板 / 未实现**；判据阈值零改动；未触碰任何生产 `.py` / `config.json`。

**2026-09-16 · §九 三项前提已核对齐 → 状态 🟢 已拍板/未实现 → 🟢 已拍板 / 实现中（D-lite 窗口执行）**

Summer 09-16 09:39 下令「开跑」，09:49 / 10:1x 两次拍板（A：抢今天窗口写实现并刷新；A：T2 七列联网补跑）。动作 #2 的执行顺序**已按计划走完前两段**：

- ✅ 受控一次批量刷新 17 序列（16 刷新 + BK0457 已到目标跳过，09:51:04–09:51:51，零失败零降级）→ `output/ops_runs/2026-09-16-dlite-refresh.md`。
- ✅ 面板与指纹**同批产出**：面板 22935 行 / 1549 交易日（`forecast_outputs/panel_dlite_20260916.jsonl`，sha256 `65902cf3…`）+ 指纹 `--with-sector`（stock 240 / fund 4 / sector 65，aggregate `b5b25cd3…`）。§五 第 3 条「实施检查项」已兑现：BK0457 原不在指纹遍历集 → `kline_fingerprint.py` 增 opt-in 板块扫描（只扩清单、哈希算法不动、默认口径逐字节不变，提交 `4882a3e`）。
- ✅ **T1 PASS**：两次生成 sha256 逐字节一致。
- ✅ **T2 PASS（口径见预注册修订节 R4）**：fwd\* 标签列 20226 格 max|Δ|=0.0；7 生产特征列经 621 请求联网重放实测 FAIL，**归因生产链路前复权改写（166/240 只），与面板实现无关**（面板不算这 7 列）→ R4 将其降级为参照项，阈值零改动。
- ✅✅✅ **T3 = FAIL（2026-09-16 11:2x–11:4x 实测，零网络，用时 22.2min）** → 已按 **R2 之二「首次不过」四步**执行：
  ① 候选槽（C′ regime_cond）**停止**，未跑任何 G2 结论；
  ② 缺口如实报告（`output/ops_runs/2026-09-16-t3-panel-power.md`）：主判据面板 14 池 **MDE80 = 0.0651 > 0.05**（距阈值 +0.0151，即还需 ~1.3× 功效），且 I 类错误实测**轮级误报 6/50 = 0.12 > 0.10 → bootstrap 失真、MDE 作废、降级「尺子不可信」**（继承 M0 同判据）；参照判据 B 单独 PASS（日 IC 重抽样 sd 0.0214 < 0.0307，降 1.43×）——**B 救不了 A**，阈值零改动、未放宽；
  ③ 自动登记 **PR-20260914-02**（本表新条目 = 预注册 R2 之二写死的下一动作）；
  ④ 剩余预算转 T1/T2 复现 + 指纹留档：T3 自检内复验面板 sha256 `65902cf3…` 与 meta 一致（T1 复现成立）、同批指纹 aggregate `b5b25cd3…` 在册；**面板显式标注 = 未通过功效检验的中间产物，不得用于任何候选裁决**。
- ⏸ 动作 #1 的「未齐不开跑」前提已满足；候选槽（C′ regime_cond）在 T3 达标前**仍不开**，与本条「明确不做」一致。
- 新增实现（全在 `experiments/forecast_lab/`，未碰 config / registry / 生产 `.py` / 定时任务）：`refresh_panel_cache.py`（`365eb70`）、`build_panel_dlite.py`（`c10126f`）、`t2_prod7_check.py`（`0b57117`）、`run_t3_panel_power.py`（T3，本次）。

---

## PR-20260914-02 · T3 功效不达标的事前指定下一动作：面板宽度再扩档 / 折构造调整（2026-09-16 自动登记）

- **登记日期**：2026-09-16（依据 = 候选 D 预注册 R2 之二 ③「自动登记」条款，非临场发明）
- **状态**：🟡 **已登记 / 未拍板实施方案**（本条只登记「事前指定的下一动作」；做哪个方向须新预注册 + Summer 拍板）
- **紧迫度**：高（09-18 候选槽被其阻塞；窗口内不解决 → 按 R2 之二精神挂 09-26 与 D-full 并案）

### 触发条件（已兑现）

候选 D 预注册 §六 T3 首次不过。2026-09-16 实测：MDE80=0.0651 > 0.05 且轮级误报 0.12 > 0.10（尺子失真、MDE 作废）。

### 触发时必须做的动作

1. 二选一（或并列）写成**新预注册文**（判据先写死）后方可动手：
   - **方向甲 · 面板宽度再扩档**：主池 14 → 更多主题代理（须逐个过 basket 闸门；relaxed 分层纪律不变，只进「分层并列」不进混池单数）。
   - **方向乙 · 折构造 / 重抽样单位调整**：window_days=63 改档、折数调整，或门槛②的日块定义变更。⚠️ §七 写死「OOS start 2025-04-30 不动」，任何改动等于换 estimand，须单独拍板，不得当作「调参」顺手改。
2. 任一方向**禁止**触碰三件写死事项：MDE80 阈值 0.05、bootstrap B=1000 / seed=42、relaxed 不混池（R2 之二「禁止项」逐字继承）。
3. 若 09-18 前无法完成新预注册 + 复跑 → 本窗口 D 侧停摆至 **09-26 季度重估**，与 D-full 并案，届时提请 Summer 确认。

### 已知约束（09-16 T3 实测带来，写进新预注册前不得忽略）

- 池宽 14→17（并入 relaxed）的敏感性参照：**MDE80 仅 0.0651 → 0.0584，仍不过** → 单纯把 relaxed 塞进混池不够，且违反分层纪律；方向甲要有效需要**更大的宽度增量**，代价是新代理的闸门核验与 PR 类风险重评。
- **轮级误报从旧池 0/50 变为面板 6/50**：失真不只来自样本量——日块行数变大后 pct2.5 严格判出的校准行为变了（旧池 923 行 vs 面板 4428 行 / 332 日块）。折构造与重抽样单位的交互是新预注册必须正面处理的对象，不得只盯 MDE 数字。
- 面板基线本身弱且不稳：pooled T+5 IC +0.0235，逐折 [−0.061, −0.009, +0.186, +0.112, −0.026]（折间翻转复现 09-12 审查对旧池的同一批评）——功效之外**信号稳定性是第二个独立问题**，扩档前如实报告，不得为凑显著挑折。

### 证据与复现

- 脚本：`experiments/forecast_lab/run_t3_panel_power.py`（判据、池定义、零网络守卫写死在 docstring；selftest 16/16）
- 产物：`forecast_outputs/t3_panel_power_20260916.json` + `t3_run_20260916.log`；报告：`output/ops_runs/2026-09-16-t3-panel-power.md`
- 上游：`output/forecast_lab_prereg_D_panel_20260914.md` §六 T3 + R2 之二（本条登记依据原文）

### 明确不做

- ❌ 不调 0.05 阈值 / 不改 B=1000 / 不改 seed=42 / 不以「趋势向好」跳过 T3。
- ❌ 不在新预注册拍板前跑任何候选槽（C′ regime_cond 维持冻结）。
- ❌ 不把本面板用于任何 G2 裁决（中间产物标注在案）。

### 修订记录（追加，不覆盖原文）

**2026-09-16 13:2x · §动作 #1 的「二选一写成新预注册」已产出两份草案（Summer 指令「甲乙并列各写一份」）**

- 乙（D2b · 修尺子）：`output/forecast_lab_prereg_D2b_ruler_draft_20260916.md` ——
  estimand/手段分界写死；探针矩阵 v0（N_PERM 50→200 钉死 0.12 读数）/ v1（moving block 5 日）/
  v2（两阶段按折），选择规则与「三 variants 全不合格即封顶挂 09-26」先写死；零网络，今晚可挂机（机时 ~3.5h）。
- 甲（D2a · 买宽度）：`output/forecast_lab_prereg_D2a_width_draft_20260916.md` ——
  标度律实测（MDE∝1/√S，14→17 预测 0.0590 vs 实测 0.0584）→ 反推达标需主池 ≈24；
  候选 10 码清单 + 同主题第二代理明示不算宽度 + 相关性自动淘汰（max|ρ|>0.85）写死；
  ⚠️ 其 §六 生效前提 = **必须用 D2b 修好的尺子复跑**，故执行顺序客观上乙先行/并行。
- 两草案均为 🟡 未拍板；拍板前本表状态维持「已登记 / 未拍板实施方案」，候选槽继续冻结。

**2026-09-16 13:40 · Summer「按你的建议执行」→ 两草案转为 🟢 已拍板；D2b 探针挂机执行中**

- 指令解读（对话上下文：我方建议 = ①今晚跑 D2b 探针挂机 + ②勾 D2a 默认名单）→ 视为对两草案**判据段**的拍板；
  两份草案各自追加 R1 修订记录（判据段冻结 + 名单勾选表 + 执行记录）。
- **D2b 执行**：`experiments/forecast_lab/run_d2b_ruler_probe.py`（selftest 13/13）；
  存量 `run_m0_power.PairedBoot` 加向后兼容 `picks=None`（默认路径逐字节不变，已断言）。
  冒烟（n_perm=4）**v0 三重复刻今日 T3 全部命中**（IC +0.0235 / ic_sd 0.0214 / MDE80 0.0651 / ρ0.10 power 0.920）
  → 证明探针口径与 T3 同构，未偷换参数。全量（n_perm=200 × 三 variant）挂机，预计 ~62 分钟。
- **D2a 名单**：9 码已勾选（510300/512100/510500/513100/159928/512690/512170/515050/510050）；
  **518880（黄金ETF）单列待 Summer 单独确认**（商品类超出 basket 攻防框架，执行方不自行入池）；
  第二代理 8 码仅备胎、首根>2020 的 3 码进 relaxed 分层。
- 执行顺序不变：今晚 D2b → 明晨选定 variant → **明日 09:30 晨检后**受控采购（今日 11:20 R3 硬线已过，
  今日零东财请求）→ 面板 v2 → T3 v2 复跑。若 D2b 判「尺子不可修」→ D2a 自动作废，甲乙并案挂 09-26。
- 本条状态：🟡 **已登记 / 已拍板实施方案（D2b 执行中）**；候选槽**继续冻结**（须待 T3 三判据全过）。

**2026-09-16 14:3x · D2b 探针跑完 → 方向乙闭环（结论=不需修尺子），路径收敛到方向甲**

- 结果（42.4min，N_PERM=200，三 variant）：**v0 现状 误报 13/200=0.065 ≤ 0.10 → 合格**；
  v1 moving block 修钝（power 0.920→0.580，塌缩 37%）→ 淘汰；v2 分层合格→按规则选定但与 v0 实质等价。
  报告 `output/ops_runs/2026-09-16-d2b-ruler-probe.md`；产物 `forecast_outputs/d2b_ruler_probe_20260916.json`。
- **关键修正**：T3 的「尺子失真（0.12>0.10）」= **50 轮分辨率不足造成的伪信号**
  （P(≥6|n=50,p=0.05)=0.038；200 轮下后 150 轮 7/150=0.047≈名义 5%）→ **该判定撤销，MDE 恢复有效**。
- **T3 最终状态**：**FAIL 仅因判据 A**（MDE80 0.0651 > 0.05）；判据 B ✓、尺子 ✓。功效缺口为真。
- **本条处置**：①方向乙（修尺子）**已闭环、不再阻塞任何事**（结论=不需要修）；
  ②方向甲（买宽度）为**唯一有效路径**，与 D2a §〇 标度律反推（≈24 成员）一致；
  ③**不触发** §动作 #3 的「挂 09-26」封顶条款（§二 规则要求三格全不合格才封顶，未触发）。
- 明日按 D2a 排期执行：09:30 晨检后受控采购/刷新 → 面板 v2 + 入池核验 → T3 v2 复跑
  （建议口径 v2，并列报 v0 以保历史基线可比；两口径结论一致）。待 Summer 单独确认 518880。
- 本条状态：🟡 **已登记 / 甲方向待执行（D2b 已闭环）**；候选槽**继续冻结**。
