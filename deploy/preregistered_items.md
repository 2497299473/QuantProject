# QuantV1 预注册待办登记表

> 用途：登记「已确认要做、但**尚未实现**」的预注册事项。登记 ≠ 实现；实现前不得静默改变口径。
> 创建：2026-09-11（Summer 要求「只登记、不实现」）。存放在 `deploy/` 的理由与 `daily_summary_task.md` 一致：本目录 git 内可追溯。
>
> **登记纪律**
> - 只增不改历史条目；条目状态变化时追加修订行，不覆盖原文。
> - 每条须含：触发条件 / 受影响范围 / 触发时必须做的动作 / 已知约束 / 证据 / 明确不做。
> - 任何实现都必须走完整流程：**先把判据写死（预注册）→ 再跑回测 → 再裁决**，不得临场改规则。
> - 本表不构成投资建议。

---

## PR-20260911-01 · 回测窗口跨 2018-06-26 ~ 2019-07-17 时必须处理 002112 分红除息

- **登记日期**：2026-09-11
- **状态**：🟡 **已登记 / 未实现**（截至登记日：未改任何生产 `.py`、未改口径、未换数据源）
- **紧迫度**：低（当前所有在跑结论不受影响，见「不受影响范围」）

### 触发条件

任何以**单位净值日收益**为输入的因子或回测，其滚动窗口或样本区间落在 **2018-06-26 ~ 2019-07-17（CST）** 之间时，本条生效。

### 受影响范围

- **仅 002112**。池内另 3 只（025687 / 022853 / 002207）已实施分红为 **0**（各 1 条记录但除息日为空），实测近 5 年单位 vs 复权日收益分歧 **0 / 1215、0 / 174、0 / 415**。
- **不受影响范围（重要，勿扩大化）**：002112 近 5 年 1216 条单位/复权日收益分歧 **0 条** → 现有 60 日 OOS、2021-2023 衰减分析、v7 基线**均不含污染日**，无需回溯修正。

### 事实底数（002112，共 5 次分红）

| 除息日 | 除息前 → 除息日单位净值 | 表观涨跌 | 每份派现 | 其中派现占比 | 剔除派现后 |
|---|---|---|---|---|---|
| 2018-06-26 | 1.1275 → 1.0771 | **-4.47%** | 0.0480 | 4.26pp | -0.21% |
| 2018-09-26 | 1.0634 → 1.0264 | **-3.48%** | 0.0400 | 3.76pp | +0.28% |
| 2018-12-25 | 1.0117 → 1.0010 | -1.06% | 0.0100 | 0.99pp | -0.07% |
| 2019-05-31 | 1.0418 → 1.0316 | -0.98% | 0.0100 | 0.96pp | -0.02% |
| 2019-07-17 | 1.0474 → 1.0074 | **-3.82%** | 0.0400 | 3.82pp | +0.00% |

- 派现占比与表观跌幅几乎完全吻合 → 这些「下跌」是分红除息，**不是市场损失**。
- 合计派现 **0.148 元/份**；同花顺复权/单位 = 5.9387/5.1610 = 1.1507（+13.1%），与派现累计幅度同量级 → 数据自洽。
- 生产口径：`core/data_loader.py` 解析东财 pingzhongdata `Data_netWorthTrend`（**单位净值**，未复权）；`lsjz` 的 `acc_nav` 仅用于取申赎状态，未参与任何计算。

### 触发时必须做的动作（实现时须预先写死判据，三选一）

1. **改用复权净值**（口径最干净，但等于更换计算输入，须重跑并标注「口径变更日」）。
2. **按派现加回**：除息日 `r = (nav_t + 每份派现) / nav_{t-1} - 1`（改动最小，仅影响 5 个日收益点）。
3. **窗口剔除该 5 日**（最省事，但丢样本，须在报告中显式注明）。

> 三者利弊与选择应在实现当次的预注册文中写死，不得跑完再挑。

### 已知约束（实现前必须解决）

- 同花顺 `performance/nav` 的 `range` 上限为 `fyear`（≈5 自然年，实测最早 2021-09-10）→ **无法从同花顺获取 2018-2019 的复权净值**。若选方案 1，须另找复权源或按本条派现表自算。
- 本地单位净值序列**不含分红标记**，无法自行发现除息日 —— 除息日与每份派现金额来自同花顺 `corporate-actions/dividends`。
- 未审计其它公司行为（份额折算 / 拆分等）；若日后需要，同法扩展。

### 证据与复现

- 完整诊断报告：`output/nav_dividend_audit_20260911.md`（只读，未改代码）
- Obsidian 笔记：`量化交易工具\盘中估值验证与同花顺源评估-20260911.md` §六
- 复现脚本：`OpenSquilla .fetch/audit_all_pool.py`（池内 4 只全扫）、`check_exdiv.py`（除息日本地核对）、`probe_div_raw.py`（除息日原文）、`adj_vs_unit.py`（单位 vs 复权）

### 明确不做（登记时的边界）

- ❌ 不替换主数据源（东财仍为净值主源）。
- ❌ 不改变现有 10 维打分输入口径。
- ❌ 不改任何生产 `.py` / `config.json` / 定时任务表。
- ❌ 不因本条而回溯修正任何已发布结论（近 5 年无污染日）。

### 待 Summer 决定（可选）

是否把本条接入 `daily_summary_task.md` 的「待拍板清单」过滤条件，使其在窗口临近时自动浮出。该改动会修改自动化任务行为，**未执行**，留待 Summer 拍板。

---

### 修订记录（追加，不覆盖原文）

**2026-09-11 13:22 · 上方「待 Summer 决定」已拍板并执行**

- Summer 选定 B + C：接入自动浮出 + 提交入库。
- 已修改 `deploy/daily_summary_task.md`：第四步「入列标准」新增末条（预注册登记表触发）+ 新增「**预注册浮出细则**」段 + 额度段补排序与不得静默丢弃规则。
- **接入方式的边界**（保留原提请时承诺的克制）：
  - 日汇总任务对本表**只读**，不得改写状态；🟡→🔴 推进只能由 Summer 人工执行。
  - 触发判断**只允许复用当天已收集的运行证据**（zcode_runs 小节 / shadow 末行 / 新报告文件名），**禁止为浮出触发而新增任何数据请求或运行 QuantV1 脚本** —— 避免为一条低紧迫度待办去撞东财频控（AGENTS.md 铁律第 1 条）。
  - 预注册项与其它待拍板项共用「每天最多 3 条」额度，但不得被静默丢弃（最多降入「其他」行）。
- 本条状态**仍为 🟡 已登记 / 未实现**：接入浮出机制 ≠ 实现分红处理，生产净值口径未变。
