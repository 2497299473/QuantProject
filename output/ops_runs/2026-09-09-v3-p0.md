# QuantV1 · V3 数据层 P0-1/2/3/4 实施报告

scope=prod codes=BK0428,BK0457,BK0669,BK1173
执行方：OpenSquilla cron agent_turn 一次性任务（job 10118c3d-bf89-4afc-b7c1-21ec5d88199f）
依据：output/V3_data_layer_plan_20260908.md 第三节实施清单、第四节验收。
本目录为 OpenSquilla 运维留痕（非 zcode_runs，避免被误读为 ZCode 产出）。

---

## 运行 2026-09-09 08:30–08:46 +08:00（本 job 第 1 轮）

### 0. 幂等检查与开工基线

- R 判定**不存在**，三路互证：`list_dir`（FileNotFoundError）、`read_file`（FileNotFoundError）、`Test-Path`（DIR_ABSENT），且 `git status` 无任何 `output/ops_runs/` 条目。→ 走「完整实施」分支。
- 回滚基线 HEAD = `061aad45e1a305d93bb2ab54efc7c2d5d62cd377`（含 159e4b9 P0-0 与 061aad4 fallback/None 防御）。
- 拉取重试计数：本 job 累计 **1 / 2**（08:43 那轮为第 1 次；昨日 23:32 属上一会话手动操作，不计入本 job）。

### 1. Phase A —— 审计确认（不重做）

开工时发现 09-08 夜 23:01 已有一轮 Phase A 改动停留在**工作树（未提交）**，备份 `backups/v3_p0_2301/`（4 文件）。本轮逐文件审计，四项全部合规，未重写生产代码：

| 项 | 要求 | 实测证据 | 判定 |
|---|---|---|---|
| P0-1 | `--scope prod/research/full`，prod 写死常量 4 码， evening/burst 同语义默认 prod，报告首行 scope=/codes= | `pull_sector_klines.py:63-72` `PROD_CODES` 常量（不从映射表解析）；evening:83、burst:64 默认 prod；报告首两行含 scope=/codes=（09-08 23:32 与本轮 09-09 两份 md 实测均含） | ✅ |
| P0-2 | beg=last+1；前缀合并；断言（严格升序无重复＋逐位相同前缀）不过 → FALLBACK_FULL；rec 加 pull_mode/history_verified_at；每周一 prod 强制全量 | `plan_beg`/`merge_klines`/`make_rec`/`is_weekly_full_day`（pull_sector_klines.py:117-204）；失败不写缓存（:279-284） | ✅ |
| P0-3 | evening ~125 行硬编码「Windows 计划任务」去掉 → `--trigger {scheduler,manual}` 默认 manual；ps1 加 `--trigger scheduler`（只改文件不重注册）；日志行「执行方式=」 | evening:85-86 参数、:147-157 写入行；grep 无硬编码来源（T4 断言 PASS）；ps1:35-45 仅改文件，本轮**未执行注册**；实机任务仍为旧参数直至下次注册，符合边界 | ✅ |
| P0-4 | 代际名只写权威文档，禁改 config.json | `git diff -- config.json` 为空；README.md「代际命名（V3 · P0-4）」行为唯一权威落点（工作树） | ✅ |

本轮补备份：`backups/v3_p0_0839/`（pull_sector_klines.py、evening、c2 夹具原件）。

### 2. Phase B 离线验收 —— 全绿

**B-1 unittest**：`.\.venv\Scripts\python.exe -X utf8 -m unittest tests.test_market_context -v` → `Ran 20 tests … OK`（**20/20**，EXITCODE=0）。

**B-2 冻结指标 diff（含一处验收缺陷的自查与纠正）**：
- ⚠️ 自查发现昨晚夹具 `v3_p0_diff_20260908.py` 比对的是快照中**不存在**的 `snap["quality"]` 键（聚合指标在顶层、质量块名为 `market_context_quality`），九项聚合指标实为 `None==None` 伪通过；theme 层 13/13 逐项比对真实有效。
- 修正版 `experiments/v3_p0_diff2_20260909.py` 重跑（`compute(as_of_date=2026-09-08, save=False)` vs `data/market_context/2026-09-08.json`）：**PASS，冻结指标零变化**——
  - 顶层 spread_5d=0.031011844149263945 / spread_original_5d=-0.2518382841654683 / breadth 三口径 0.3846/0.4/0.3333 / regime=neutral / ok / as_of：9/9 SAME（old 侧非 None 8/9，比对非空）；
  - `market_context_quality`：coverage=1.0、usable_for_oos=True、errors=0、themes_switched=0、13/10/3、oos_quality=ok 等 9 项全 SAME；
  - themes 13/13：r1d/r5d/r20d/above_ma20/as_of/code/proxy_switch/gate/data_source 逐项零漂移；
  - bars 差异仅 创业板/红利 各 −1（3196→3195）：**归因** = 09-08 22:02 探针触发 `_TTL_HOURS=12` 过期后腾讯 ETF 正常重拉（`v3_p0_bars_attrib` 实测 13 个缓存 mtime 晚于快照 14:55，腾讯定长窗口头部裁切）；bars 为输入深度诊断量、非冻结口径。
  - P0-0 新增键（max_lag_trading_days/stale_themes/data_ref_last/calendar_gap_days）记 ADDED，属 159e4b9 既有授权范围。

**B-3 history.jsonl / 快照不变性**：sha256 `fdfe7f682581b1997bfdb5f89958ec599dddb64e9702fa79bc7f1241ae6983ad`、3 行、mtime 09-08 10:42:41——全部离线验收**前后全同**；`data/market_context/` 目录内 5 文件哈希清单（含 4 份快照）运行前后 UNCHANGED；Phase C 拉取失败后再次复核仍 UNCHANGED。**不新增行、原行未被改写 ✅**。

### 3. Phase C 拉取 + 网络验收 —— 受阻（东财封禁持续）

08:43:23 实机确认前置窗口后执行第 1 轮：`.\.venv\Scripts\python.exe -X utf8 pull_sector_klines.py --scope prod`

- 拉取前防双跑核查：ZCode `C:\Users\Turn-\.zcode\v2\tasks-index.sqlite` 以「复制副本→只读查询→源文件前后 sha256 一致」方式核查（SOURCE UNTOUCHED: True），automations 表仅 3 条，其中 K 线任务 `QuantV1 板块 K 线每日增量` cron `0 16 * * 1-5`——08:43 不在执行窗口；Windows `QuantFund_KlineEvening` State=**Ready**、LastRun=09-08 21:30(Result=0)、Next=09-09 21:30，未在跑。
- 结果（output/pull_sector_klines_20260909.md 实测）：**ok=0 skip=0 fail=4 / 4**，4/4 `RemoteDisconnected:Remote end closed connection without response`。
  - FAIL 码全量记录：**BK0428, BK0457, BK0669, BK1173**（beg 分别为 20260909/20260909/20260908/20260909——增量规划路径本身正确生效）。
  - 按铁律：未换 IP、未加 retry、未做第 2/3 轮；单轮请求数=4。
- 数据完好性：BK0457/BK0428/BK1173 last=2026-09-08、BK0669 last=2026-09-07（缺口两日），四码 mtime 均早于本轮 → **失败零写入、无半成品**；BK0457 first=2015-01-05、covers_oos=True 不变；当前生产数据尚不触发 stale（as_of=09-09 的期望末根=09-08 已满足），若 09-10 仍全败 stale 检测将亮灯。
- 网络类验收（4 码 last==当日、实时增量对照）顺延至下一窗口。

**C-2 等价性以离网证明替代（真实缓存序列，4 码 × 截断 1/5/21 根全部还原全量一致，12/12 PASS）**，并修正夹具两处缺陷后 `fails: 0`：
1. 「历史修订→MergeRejected」原用例把修订写进本地旧缓存再喂 date>last 应答——**按构造不可判定**，改为可观测签名「服务端回传 date≤last 且异值 → overlap_mismatch」后 PASS；同时补一条「本地缓存被改不抛错=已知局限」＋「兜底=周一 prod 强制全量」留痕；
2. 首次修正误把篡改点放在 revsrv[1]（日期>last 属合法新增），改至重叠区 revsrv[0] 后通过。
- payload 对比：全量窗口（beg=20150101）应答 2840 bars ↔ 增量窗口（beg=last+1）应答 4 bars，**降幅 99.86%**；单日请求数 65→≤4 ✓。

### 4. 验收清单对照（计划文档第四节）

| # | 项 | 状态 |
|---|---|---|
| 1 | BK0457 last==当日 / covers_oos / first 不变 | ⏳ 顺延（封禁）；first/covers_oos 现已实测不变 |
| 2 | 增量 bars==全量 | ✅ 离网 12/12 等价证明（实时复核顺延） |
| 3 | 冻结指标零漂移 | ✅（修正夹具层级后真实通过） |
| 4 | history.jsonl 不新增/不改写 + unittest 20/20 | ✅ / ✅ |
| 5 | 请求数 65→≤4、beg 12年→1天 | ✅（4 请求、beg=20260909/20260908，payload 留档） |
| 6 | 断言失败自动 FALLBACK_FULL 不产生半成品 | ✅ 代码路径+夹具证明 |

### 5. 遗留与提示（供 Summer / 下轮）

1. Phase A 改动**仍在工作树未提交**（16:00/21:30 直接拾取工作树脚本，不依赖提交）；是否入库由 Summer 定。
2. BK0669 已缺两根（09-08、09-09）；通道恢复后该码将以 beg=20260908 一次补齐。
3. 下周一 09-14 prod 将自动触发全量覆盖校验（历史前缀终极防线）。
4. `install_scheduled_tasks.ps1` 的 scheduler 参数需下次**实际重注册**才生效（本轮按边界未重注册）。
5. 建议下轮窗口：今日 16:00 ZCode 任务（将按新 prod=4 码口径运行）或 21:30 KlineEvening 之后；本 job 保留待重试（剩余重试预算 1 次）。

---

## 运行 2026-09-09 08:40–08:49 +08:00（本 job 第 2 轮；并发实例，只重跑 Phase C）

### 0. 幂等判定（与第 1 轮的时序冲突，如实留痕）

- 本实例 08:41 开工检查：R **NOT_EXISTS**（`if exist` 实测）→ 当时按规则只能走「完整实施」分支，回滚基线 HEAD=`061aad45e1a305d93bb2ab54efc7c2d5d62cd377`。
- 08:47:28 R 被第 1 轮实例写入并置 PENDING_PULL；本实例 08:49 收尾复核时才发现 → **同一 at 型 job 在 08:30 与 08:40 被两次触发**。本实例不覆盖第 1 轮任何内容，仅追加本节。
- 拉取尝试合并计数（本 job）：第 1 轮 08:43:23 + 本实例 08:46 = **2 次，预算 2/2 已用尽**。两实例未重叠执行（本实例 08:45:40 实测无真实 pull 进程、缓存 10 分钟内零写入），但 3 分钟内共向东财发出 8 个请求。

### 1. Phase A —— 只复核、不重做（结论与第 1 轮一致）

| 项 | 本实例独立取证 | 判定 |
|---|---|---|
| P0-1 | `PROD_CODES` 为写死常量（pull_sector_klines.py:63-72，注释明示不得从映射表解析）；`load_codes` 默认 prod；evening:83 / burst:64 默认 prod；报告首两行含 `scope=prod codes=BK0428,BK0457,BK0669,BK1173` | ✅ |
| P0-2 | `plan_beg`（:117-137，含 skip/full 五种回退触发）、`merge_klines`（:144-167，overlap_mismatch / not_strictly_ascending / prefix_not_identical 三类拒绝）、`make_rec`（:170-194，含 `pull_mode`/`pull_beg`/`history_verified_at`/`history_full_verified_at`）、`is_weekly_full_day`（:197-204，仅 prod、仅周一）、失败不写缓存（:281-284） | ✅ |
| P0-3 | evening:85-86 `--trigger {scheduler,manual}` 默认 manual、:141-160 写入「**执行方式**=%s」+ 任务状态快照，grep 已无「Windows 计划任务」硬编码来源；ps1:32-42 含 `--trigger scheduler`，**本轮与第 1 轮均未重注册**（符合边界） | ✅ |
| P0-4 | `git status --porcelain core/ config.json` 输出为空 → config.json 仍 `"version":"4.0.0-decision"` 未被触碰；README.md:12 为代际名唯一权威落点 | ✅ |

本实例新增备份：`backups/v3_p0_preC_20260909-0846/`（65 个板块缓存全量，prod 4 码与实机 sha256 逐位相同）。

### 2. Phase B —— 独立离线复核（全绿）

- **B-1**：`.\.venv\Scripts\python.exe -X utf8 -m unittest tests.test_market_context` → `Ran 20 tests in 8.151s / OK`（**20/20**）。
- **B-2**：层级修正版夹具 `experiments/v3_p0_diff2_20260909.py`（`compute(as_of_date=2026-09-08, save=False)` vs `data/market_context/2026-09-08.json`）→ **PASS(冻结指标零变化)**：顶层 spread_5d=0.031011844149263945 / spread_original_5d=-0.2518382841654683 / breadth 三口径 0.38461538461538464·0.4·0.3333333333333333 / regime=neutral / ok=True / as_of 全 SAME（old 侧非 None 8/9，排除伪通过）；`market_context_quality` 9 项（coverage=1.0、usable_for_oos=True、errors=0、themes_switched=0、13/10/3、oos_quality=ok、themes_bk_source=1）全 SAME；themes 13/13 的 r1d/r5d/r20d/above_ma20/as_of/code/proxy_switch/gate/data_source 零漂移。
  - bars 差异仅 创业板 / 红利 各 −1（3196→3195）：**本实例直接归因到缓存本体** —— `159915.json` mtime 09-08 22:02:12、`510880.json` 22:02:17，两者 klines 实长 3196、末根 2026-09-08（腾讯定长窗口头部裁切）；属 `_TTL_HOURS=12` 探针重拉，bars 为输入深度诊断量、非冻结口径。`git status` 证明 `core/market_context.py` **零改动**，计算路径未被本轮触碰。
- **B-3**：`history.jsonl` = 3 行、sha256 `fdfe7f682581b1997bfdb5f89958ec599dddb64e9702fa79bc7f1241ae6983ad`，验收前后全同；`data/market_context/` 目录 5 文件哈希清单 UNCHANGED（`2026-09-08.json` = `45e4334354813bbcfec0fbaaf5df997bccf3c976130fdb37e8a0356f08e35607`）；Phase C 全败后再次复核仍 UNCHANGED → **不新增行、原行未被改写** ✅。

### 3. Phase C —— 第 2 轮拉取仍全败（东财封禁持续）

- 前置防双跑核查：ZCode `tasks-index.sqlite` 以 `mode=ro` 只读查询（**未发起任何写入**），`QuantV1 板块 K 线每日增量` cron `0 16 * * 1-5` → running=0 / dispatch=idle / next=09-09 16:00；`QuantFund_KlineEvening` 模式=就绪、上次 09-08 21:30 结果=0、下次 09-09 21:30；无其他拉取进程、板块缓存最新 mtime 停在 09-08 20:06 → 判定窗口安全。
- 执行：`.\.venv\Scripts\python.exe -X utf8 pull_sector_klines.py --scope prod`（08:46）→ **`== 汇总：ok=0 skip=0 fail=4 fallback_full=0 / 4 ==`、`== 东财请求数=4（scope=prod；改造前 full 口径每轮 65）==`**
- FAIL 码全量记录：**BK0428, BK0457, BK0669, BK1173**，错误签名统一 `RemoteDisconnected: Remote end closed connection without response`。
- 增量改造生效（即使失败也留下正确规划证据）：四码 `mode=incremental`，`beg=20260909`（BK0428/BK0457/BK1173，last=09-08 的次日）/ `beg=20260908`（BK0669，last=09-07）→ **12 年窗口已压成 1 个日历日**；单日请求数 **65 → 4**。
- 按铁律：未换 IP、未加 retry、未做第 3 轮拉取。
- 失败零写入复核（08:48）：`data/sector_klines` 65 文件**今日零改动**；BK0457 `bars=2840 / len(klines)=2840 / first=2015-01-05 / last=2026-09-08 / covers_oos=True`、mtime 仍 09-08 19:19:20；BK0428 last=09-08；BK0669 last=2026-09-07（缺 09-08、09-09 两根，通道恢复后 beg=20260908 一次补齐）；BK1173 `covers_oos=False`（2024-04-17 起，符合"仅作通道健康探针"定位）→ **无半成品缓存**。
- 网络类验收（BK0457 `last==当日`、增量与全量实时对照）**未达成**，顺延；第 1 轮的离网等价证明（12/12 PASS）仍是该断言的最佳替代。

### 4. 重试预算与终局动作

- 本 job 拉取重试累计 **2 / 2 已用尽** → 按铁律**停止重试、保留本 job 交 Summer 处置**，不删除、不新建任何任务、不做第三轮拉取。

### 5. 待 Summer 决策（本轮新增/强化）

1. **单源封禁已成事实**：09-08 16:00 / 17:53 / 19:10 / 20:05 / 21:30 + 09-09 08:43 / 08:46 连续 7 轮全 fail，BK0457 无零成本备份源。今日 16:00（ZCode）与 21:30（Windows）会自动按新 `prod=4` 口径再试，无需人工补拉；若届时仍全败，问题已从「频控」升级为「出口/账户级封禁」，需另行决策。
2. **一次性 job 并发双触发**（08:30 与 08:40 两个实例互不知晓）：建议核对 at 型 agent_turn job 的领取/去重逻辑，否则重试预算会被并发消耗（本例即 2/2 直接见底）。
3. **ps1 与实机任务不一致（本任务边界内不可修）**：实机 `QuantFund_KlineEvening` Arguments 仍为 `-X utf8 "...pull_sector_klines_evening.py"`，**无 `--trigger scheduler`**。脚本默认为 manual → 今晚 21:30 日志会如实记「执行方式=manual」而非 scheduler。要拿到正确的 scheduler 标注，需授权后实际执行一次 `install_scheduled_tasks.ps1` 重注册（任务明令本轮不得重注册）。
4. **stale 亮灯预期**：09-09 期望末根=09-08，BK0457/BK0428/BK1173 已满足，今日不亮灯；若 09-10 仍全败，P0-0 陈旧检测将把电网设备记入 `stale_themes` 并使 `usable_for_oos=false` —— 届时污染样本会被自动挡在 OOS 之外（这正是 159e4b9 的预期行为）。
5. Phase A 改动**仍在工作树未提交**（16:00 / 21:30 直接拾取工作树脚本，不依赖提交）；是否入库由 Summer 定。

---

## 运行 2026-09-09 08:51–08:56 +08:00（本 job 第 3 轮；零拉取、只读复核）

### 0. 幂等判定

- `Test-Path` → R **存在**；通读全文，末行 `STATUS: PENDING_PULL`（第 131 行，全文唯一 `STATUS:` 标记）。→ 按分支规则：**跳过 Phase A，只重试 Phase C**。
- 但第 2 轮 §4 已记 **本 job 拉取重试累计 2 / 2 用尽**，且任务铁律明示「超过则停止并保留 job 供 Summer 处置」。故本轮**不执行任何拉取**（东财请求数 = 0，未新增第 3 轮）、不删 job、不新建 job、不改代码。
- 开工基线复核：`git rev-parse HEAD` = `061aad45e1a305d93bb2ab54efc7c2d5d62cd377`（与前两轮一致，未发生回滚/提交）；`git status --porcelain -- config.json core` **输出为空** → P0-4 边界仍守住（config.json version 未被触碰、`core/market_context.py` 零改动）。

### 1. 只读复核结果（prod 4 码 + 快照不变性，15:xx 前无副作用取证）

| 对象 | 实测 | 与前轮一致性 |
|---|---|---|
| BK0457 | bars=2840 / first=2015-01-05 / last=**2026-09-08** / covers_oos=True / mtime 09-08 19:19:20 | ✅ 未变 |
| BK0428 | bars=2840 / first=2015-01-05 / last=2026-09-08 / covers_oos=True / mtime 09-08 19:19:14 | ✅ 未变 |
| BK0669 | bars=2839 / first=2015-01-05 / last=**2026-09-07** / mtime 09-07 16:01:22 | ✅ 缺口仍为 09-08、09-09 两根 |
| BK1173 | bars=583 / first=2024-04-17 / last=2026-09-08 / **covers_oos=False** / mtime 09-08 16:16:25 | ✅ 未变（符合「仅作通道健康探针」定位） |
| `data/sector_klines` 今日写入 | 今日（≥09-09 00:00）改动文件 **0 个** | ✅ 前两轮失败仍为零写入、无半成品 |
| `data/market_context/history.jsonl` | 3 行 / sha256 `fdfe7f682581b1997bfdb5f89958ec599dddb64e9702fa79bc7f1241ae6983ad` / mtime 09-08 10:42:41 | ✅ 与前两轮逐位相同，不新增行、原行未改写 |

- **无拉取进程在跑**：进程表中仅 OpenSquilla gateway / 本命令自身，无 `pull_sector_klines*` 实例；故本轮不存在与 16:00 ZCode、21:30 Windows 任务的双跑风险（也未查 sqlite——本轮零拉取，前置核查不适用）。

### 2. 一处需要留痕的新观察（非缺陷，防下轮误读）

- 四码缓存 JSON 顶层 `pull_mode` 读出为 **None**，`history_verified_at` 亦不存在。原因明确：P0-2 的 `make_rec` 只在**成功拉取**时写这两个字段，而这四个缓存的最后成功写入时间（09-07 16:01 / 09-08 16:16 / 19:19）均早于 Phase A 改造落盘，属预期状态，不是改造未生效的证据。改造生效的直接证据仍是第 2 轮运行日志：四码 `mode=incremental`、`beg=20260909 / 20260908`、单轮请求数 4。
- 推论：**通道恢复后的第一次成功拉取**才会给 4 码补上 `pull_mode` / `history_verified_at`；下周一 09-14 的 prod 强制全量则会补 `history_full_verified_at`。

### 3. 终局动作与需要 Summer 决策的硬问题

1. 本轮**零拉取、零改动**，R 状态维持 `PENDING_PULL`，本 job **保留**（未 remove、未新建任何任务、未重注册计划任务）。
2. ⚠️ **保留 job 已失去自动触发前提**：`cron list` 显示本 job `10118c3d-bf89-4afc-b7c1-21ec5d88199f` 为 at 型（`2026-09-09T08:30:00+08:00`）、status=`running`。触发时刻已过且重试预算见底，任务文本要求的「靠 at 兜底/重试」在本 job 上已不可能发生——网络类验收（BK0457 `last==当日`、增量 vs 全量实时对照）实质上只能靠今日 16:00 ZCode 与 21:30 Windows 两个既有任务顺带完成，或由 Summer 显式授权一次新的补拉。本任务边界禁止我新建 job，故停在此处等决策。
3. **stale 亮灯已在门口**：BK0457 last=09-08，as_of=09-10 的期望末根为 09-09。若今晚两班仍全败，159e4b9 的陈旧检测会把电网设备记入 `stale_themes` 并使 `usable_for_oos=false`（预期防线，非新增故障）。
4. 第 2 轮提出的「ps1 与实机任务不一致（缺 `--trigger scheduler`）」仍需**授权重注册**才能闭环，本轮按边界未动。
5. Phase A 改动依旧停留在**工作树未提交**；是否入库由 Summer 定。

---

## 补记 2026-09-09 09:39 +08:00（调度迁移影响评估，Summer 问询触发；零拉取、零代码改动）

- Summer 已将 QuantV1 三条任务从 ZCode 迁回 OpenSquilla cron（09:22 建：16:00 K线=55c96e26 / 22:30 Shadow=69b20a70 / 09-26 重估=0cfaa288）；ZCode automations 表实测 **0 行**（只读副本查询，源文件前后 sha256 一致）→ 双跑风险解除。
- **本 P0 一次性 job（10118c3d）已从 cron 列表消失**（09:35 实测不在列）→ 本 R 的 PENDING_PULL 不再有 at 兜底重试；网络类验收（BK0457 last==当日、增量 vs 全量实时对照）改由今日 16:00 OpenSquilla job 与 21:30 Windows QuantFund_KlineEvening 顺带完成，成功后需人工把 R 置 COMPLETE 并补验收记录。
- 第 3 轮 §3.2 中「16:00 ZCode」表述随迁移失效：执行方现为 OpenSquilla job 55c96e26（不带 --scope，默认 prod=4 口径，与 P0-1 一致，请求预算不变）。
- 新 16:00 任务 prompt 两处陈旧文案（不阻塞，仅提示）：「ok=N skip=M fail=K / 65」实际输出为「/ 4」；「顽固失败码 BK1137/BK1163」不在 prod 4 码内。
- 21:30 Windows QuantFund_KlineEvening 为 Windows 计划任务，不属本次迁移范围，照常运行（State=Ready、Next=09-09 21:30，实机参数仍无 --trigger scheduler，见第 2 轮 §5.3）。

## 补记 2026-09-09 09:55 +08:00（Summer 授权三项全批，执行完毕）

1. **16:00 K 线 job 文案修正重建**：`55c96e26` → `fa6d9cdd`（同 schedule `0 16 * * 1-5`、同 tz=Asia/Shanghai、同 agent_turn/isolated；仅改 2 处失配：核对分母 `/ 65` → `/ 4` 并注明默认 scope=prod 4 码；顽固码 BK1137/BK1163 注明「仅在 full scope 出现」；其余逐字不变。旧 job 已 remove，新 job status=pending 待 16:00 首跑）。
2. **22:45 一次性收尾验收 job**：`243d9df4`（at 2026-09-09T22:45+08:00，09:53 重建自 `6fceb42e`）。纯只读验收：四码缓存 bars/last/first（预期 2841/2841/2841/584、last=2026-09-09）、`output/pull_sector_klines_20260909.md` 首行 scope=prod 且请求数≤4 且 beg 为 1 天窗口、unittest 20/20。全绿 → 本 R 改 `STATUS: COMPLETE` 并在收尾节写明「旧实施 job 已随其运行自动删除、cron list 复核无残留」；失败 → 追加失败点留痕，STATUS 不动，待 Summer 处置。job 自带日期守卫（非 09-09 触发即不验收、只报告过期）。
3. **ps1 实际重注册（P0-3 闭环）**：`deploy\install_scheduled_tasks.ps1` 已执行（09:47），三任务重注册成功（Mid 11:30 / Post 14:55 / KlineEvening 21:30，均 Ready）。实机 `QuantFund_KlineEvening` Arguments 现为 `-X utf8 "...pull_sector_klines_evening.py" --trigger scheduler` → 今晚 21:30 日志「执行方式=scheduler」生效。重注册前三任务完整配置已备份 `backups\win_tasks_pre_reregister_20260909\`。ps1 内「未重注册」注释已同步更新。
4. 其余不变：本 R 维持 `PENDING_PULL` 至 22:45 验收判定；Phase A 改动仍在工作树未提交（待 Summer 定）；未新建任何周期性任务、未触碰 ZCode 表、未改冻结指标与 config.json。

---

## 补记 2026-09-09 10:5x +08:00：at 型 job 双触发根因修复（Summer 指示「修复掉」）

### 1. 根因（证据链：scheduler.db 只读副本 scheduler_runs 三条记录 + v0.5.4 源码 + 离线复现测试）

- 实为 **三重触发 = 孤儿实例 + 定时重投**，不是单纯「at 型被派发两次」：
  - run1 08:30:00–08:40:00：`Timeout after 600.0s`（外层）——`execute_with_timeout`（jobs.py）的 `wait_for(handler, 600)` 与 handler 内层 `task_runtime.wait(600)`（handlers.py:320）**同值竞争**，且内层在会话建立/入队之后才起算 → 外层**必赢** → handler 协程被 cancel，其「超时→cancel runtime task→杀 agent」的 except 分支（handlers.py:321-322）**永不可达** → agent 实例 A 成孤儿继续跑（08:43 第一刀）；
  - run2 08:40:30–08:50:30：timeout 被 classify_error 归 transient → AT 一次性 job 按设计 backoff 30s 重投 → 实例 B（08:46 第二刀），B 同样被掐成孤儿；
  - run3 08:51:30–08:54 成功收尾（08:51–08:56 节）——它看到 R 已存在属正常幂等判定，不是 bug 的一部分。

### 2. 修复（升级前本地补丁，OpenSquilla v0.5.4）

- 改 `scheduler/jobs.py`：`OUTER_TIMEOUT_GRACE_SECONDS = 60.0`，外层 `wait_for` 预算 = timeout+60s → 内层超时（带 agent 取消清理）永远先开火，外层退化为「handler 自身清理挂死」时才触发的真正兜底（触发时升为 error 日志，文案含 `+60s outer grace`）。选 60s 因 delivery 链单 adapter send 上限 30s，须盖住 handler 收尾尾巴。
- 验证：离线竞态复现测试——未打补丁 `VERDICT: BUG REPRODUCED`（外层先赢、agent 未取消），打补丁后 `VERDICT: FIX OK`（内层先开火、runtime task 被取消、无孤儿），exit 0；scheduler 包 compileall 通过。
- **生效条件：补丁只在 gateway 进程启动时加载 → 需重启 gateway 才生效。重启有窗口风险（11:30 QuantFund_Mid / 14:55 Post / 16:00 cron 拉取在跑或即将在跑），留待 Summer 拍板时间（建议 11:35–14:50 之间或今日 23:05 后）。**
- 回滚：恢复 `backups/opensquilla_cron_fix_20260909/`（jobs.py/handlers.py/timer.py 原件 + jobs_patch.diff + cron_race_regression_test.py）。

### 3. 今日防护现状与残留风险

- **补丁未重启前，今晚 22:45 验收 job（`243d9df4`，只读零拉取）仍可能双触发**：预期形态 = 实例①被掐为孤儿继续跑 + 08:40 同款 30s 重投实例②；验收为只读重跑无害，但 `cron list` 可能同时出现两个 running 记录，22:45 后若见 `10118c3d 式`的成对 timeout run 属预期，勿再判为事故。R 的 COMPLETE 翻转由「末尾单行唯一 STATUS:」写死降低乱序竞态。
- **长期运维纪律（补丁重启生效前的过渡规则）**：给重活型 at/agent_turn job 显式设大 `timeout_seconds`（CLI `cron add/update --timeout` 支持；建议 ≥1800），**禁止**把 >10 分钟的活建在默认 600s 上（默认值正是本次事故的放大器）。
- 已顺手把 `243d9df4` timeout 提到 **3600s**（纯 DB 字段，不需重启即生效；验收是只读轻活，600s 内层够用，放宽只是降低孤儿窗口概率的保险）。

---

## 运行 2026-09-09 22:45 P0 收尾验收（一次性验收 job 243d9df4；全只读、零拉取）

- **过期守卫**：`Get-Date` = 2026-09-09 22:45:47 +08:00 → 日期正确，未过期，继续。
- **幂等检查**：R 存在，末行 `STATUS: PENDING_PULL`、全文无 `STATUS: COMPLETE` → 执行验收。
- **边界遵守**：本轮未执行任何 `pull_sector_klines*.py`、**东财请求数 = 0**；未新建/删除任何 cron；`tasks-index.sqlite` 未读写；config.json 与冻结指标未触碰；Windows 计划任务未重注册。

### 1. 四码缓存硬检查 —— ❌ **未过**（4 码仅 BK0457 达标）

| 码 | 名称 | bars | first | last | covers_oos | pull_mode | history_verified_at | 文件 mtime | 判定 |
|---|---|---:|---|---|---|---|---|---|---|
| BK0457 | 电网设备 | **2841** | 2015-01-05 | **2026-09-09** | True | incremental（另有 pull_beg=20260909、history_full_verified_at=2026-09-08） | 2026-09-09 16:07:56 | 09-09 16:07:56 | ✅ 全项达标 |
| BK0428 | 电力 | **2840**（期望 2841） | 2015-01-05 | **2026-09-08**（期望 09-09） | True | 键不存在 | 键不存在 | 09-08 19:19:14 | ❌ last 非当日、bars 未 +1 → **待人工确认**（实况如此，非计算差异） |
| BK0669 | 生态农业 | **2839**（期望 2841） | 2015-01-05 | **2026-09-07**（期望 09-09） | True | 键不存在 | 键不存在 | 09-07 16:01:22 | ❌ 缺口仍为 09-08、09-09 两根 → **待人工确认** |
| BK1173 | 锂矿概念 | **583**（期望 584） | 2024-04-17 | **2026-09-08**（期望 09-09） | False（按任务书：首根不覆盖 OOS 属正常，不计失败） | 键不存在 | 键不存在 | 09-08 16:16:25 | ❌ last 非当日、bars 未 +1 → **待人工确认** |

- 数据完好性旁证：四码 `len(klines)` 与顶层 `bars` 逐位一致；序列**严格升序、零重复日期**；BK0457 实测同时含 2026-09-08 与 2026-09-09 两根。
- 今日写入面：`data/sector_klines` 今日（≥09-09 00:00）改动文件 **1 个**（仅 BK0457）→ 三码失败仍为零写入、**无半成品缓存**。
- 证据项（缺失不单独判失败）：`pull_mode` / `history_verified_at` 只有 BK0457 在 16:07 那次成功拉取后补上，另三码今日全 fail 故仍无此二键 —— 与第 3 轮 §2 的推论完全一致，**反向确认改造逻辑正确**。

### 2. 拉取报告 payload 检查 —— ✅ 通过（但结果本身是部分失败）

`output/pull_sector_klines_20260909.md`（mtime 09-09 16:08:09，16:00 OpenSquilla job 产出）：

- 首行/次行：`# 板块 K 线拉取缓存 2026-09-09 scope=prod codes=4` + `scope=prod codes=BK0428,BK0457,BK0669,BK1173 force_full=False monday_full=False` → **scope=prod 且 4 码 ✅**
- 请求数：本轮 `== 东财请求数=4（scope=prod；改造前 full 口径每轮 65）==` → **≤4 ✅**（非 65）
- beg：`20260909`（BK0428/BK0457/BK1173）/ `20260908`（BK0669），四码 `mode=incremental`、`fallback_full=0` → **1 天增量窗口 ✅**，无 12 年全量（20150105/20150101）
- 结果：`ok=1 skip=0 fail=3 fallback_full=0 / 4`，FAIL 码 `BK0428,BK0669,BK1173`，错误签名 `RemoteDisconnected`
- 21:30 晚间轮（`output/pull_sector_klines_evening_20260909.md`，mtime 09-09 21:30:18，重注册后首跑）：`ok=0 skip=1 fail=3 / 4`、`东财请求数=3`、beg 同为 20260909/20260908，FAIL 码同上 → 三项 payload 断言同样满足；晚间轮亦未恢复被封通道。

### 3. unittest —— ✅ 全绿

`.\.venv\Scripts\python.exe -X utf8 -m unittest tests.test_market_context` → 输出末三行：`----------------------------------------------------------------------` / `Ran 20 tests in 7.131s` / `OK`，EXITCODE=0（**20/20**）。

### 4. 旧实施 job 残留复核 —— ✅ 无残留

`cron action=list` 实机返回 6 条：`69b20a70`（22:30 Shadow）、`fa6d9cdd`（16:00 K线）、`243d9df4`（本验收 job，status=running）、`93439da7`（09:30 晨检）、`147825cf`（09-26 季度重估）、`1d99f4e7`（09-10 P1 开工）。**不存在**以「QuantV1 · V3 数据层 P0-1/2/3/4 实施」开头的 job → 旧实施 job `10118c3d` 已随其运行被运行时自动删除，cron list 复核确认无残留，故**未执行任何 remove**。

### 5. 判定与待 Summer 处置

- **总判定：验收未全绿** —— 第 1 项缓存硬检查失败（BK0428 / BK0669 / BK1173 三码 `last` 未达 2026-09-09、bars 未 +1）；第 2 项（payload 口径）、第 3 项（unittest 20/20）、cron 残留复核均通过。按任务规则 **STATUS 行保持 `PENDING_PULL` 不动**，不翻 COMPLETE。
- 归因（非改造缺陷）：增量链路已在真实通道上端到端跑通一次（BK0457 16:07：beg=20260909 → bars 2840→2841、first/covers_oos 不变、pull_mode/history_verified_at 补齐），另三码 09-09 两班（16:08 / 21:30）仍 `RemoteDisconnected` → **东财 IP 级封禁未解除**，与 Phase A/B 改造无关。
- 后续：三码待通道恢复后由既有 16:00 / 21:30 任务自然补齐（BK0669 将以 beg=20260908 一次补两根），届时需**人工**再验一次并翻 COMPLETE；本验收 job 为 at 型且已触发，不再兜底。
- ⚠️ stale 亮灯已成事实：as_of=09-10 的期望末根为 09-09，BK0428/BK0669/BK1173 均不满足 → 明日 P0-0 陈旧检测将把对应 theme 记入 `stale_themes` 并使 `usable_for_oos=false`（159e4b9 预期防线，非新增故障）。
- 另需 Summer 留意：`install_scheduled_tasks.ps1` 重注册后的 21:30 晚间轮报告表头**无「执行方式=」行**（本次 evening md 表头仅 BK|名称|类型|mode|beg|bars|首根|末根|OOS|状态），P0-3 的「执行方式=scheduler」标注是否真落盘值得下轮顺带核一眼。

---

## 运行 2026-09-09 22:45 P0 收尾验收（同 job 第 2 实例，实际 22:54 触发；全只读、零拉取）

- **实例身份**：本 job 为 `243d9df4`（at 2026-09-09T22:45+08:00）。上一节由本 job **第 1 实例**（22:45:47）写入；本轮为**并发第 2 实例**，形态与「10:5x 补记 §3」预告的未重启竞态（外层 600s 超时掐断 handler → 孤儿实例 + transient 分类 30s 重投）一致 → `jobs.py` 的 `+60s outer grace` 补丁**仍未随 gateway 重启生效**；22:45 后成对运行属预期，非新事故。本节为**独立取证复核**，未采信第 1 实例结论，也不覆盖其内容。
- **过期守卫**：`Get-Date` = **2026-09-09 22:54:46 +08:00（星期三）** → 日期正确、未过期，继续验收。
- **幂等检查**：R 存在；`grep ^STATUS:` 全文仅第 255 行一处行首 STATUS = `PENDING_PULL`，**无** `COMPLETE` → 执行验收。
- **边界遵守**：本轮**东财请求数 = 0**（未执行任何 `pull_sector_klines*.py`、未发起任何网络拉取）；未新建/删除任何 cron；`C:\Users\Turn-\.zcode\v2\tasks-index.sqlite` 未读写；`config.json` 与冻结指标未触碰；Windows 计划任务未重注册；对 R 仅**追加**，STATUS 行未改。

### 1. 四码缓存硬检查 —— ❌ **未过**（4 码仅 BK0457 达标）

取数：`.\.venv\Scripts\python.exe -X utf8` 只读直读 `data/sector_klines/<code>.json`；`bars` 与 `len(klines)` 逐位比对 + 严格升序/重复日期校验。

| 码 | bars 实况（期望） | first | last 实况（期望） | covers_oos | pull_mode | history_verified_at | mtime | 判定 |
|---|---|---|---|---|---|---|---|---|
| BK0457 | **2841**（2841）✅ | 2015-01-05 ✅ | **2026-09-09** ✅ | True ✅ | `incremental`（pull_beg=20260909、history_full_verified_at=2026-09-08） | 2026-09-09 16:07:56 | 09-09 16:07:56 | ✅ 全项达标 |
| BK0428 | **2840**（2841）❌ | 2015-01-05 ✅ | **2026-09-08**（09-09）❌ | True | 键不存在 | 键不存在 | 09-08 19:19:14 | ❌ last 非当日、bars 未 +1 → **待人工确认** |
| BK0669 | **2839**（2841）❌ | 2015-01-05 ✅ | **2026-09-07**（09-09）❌ | True | 键不存在 | 键不存在 | 09-07 16:01:22 | ❌ 缺 09-08、09-09 两根 → **待人工确认** |
| BK1173 | **583**（584）❌ | 2024-04-17 ✅ | **2026-09-08**（09-09）❌ | False（任务书明示属正常，**不计失败**） | 键不存在 | 键不存在 | 09-08 16:16:25 | ❌ last 非当日、bars 未 +1 → **待人工确认** |

- bars 不符实况（按规则列值、不单独判失败，但计入第 1 项）：**BK0428=2840、BK0669=2839、BK1173=583**（期望 2841/2841/584）。
- 完好性旁证：4/4 `bars == len(klines)`；序列**严格升序、零重复日期**；BK0457 同含 09-08 与 09-09 两根，另三码均无 09-09 根（BK0669 连 09-08 亦无）。
- 证据项：`pull_mode` / `history_verified_at` 仅 BK0457 具备，另三码二键**均不存在** —— 与第 3 轮 §2 推论（只有成功拉取才写此二键）一致，**反向确认 P0-2 逻辑正确**。
- 与第 1 实例数值**逐项吻合**：两次独立读取互证三码今日零写入、**无半成品缓存**。

### 2. 当日拉取报告 payload 检查 —— ✅ 通过

`output/pull_sector_klines_20260909.md`（16:00 OpenSquilla job `fa6d9cdd` 产出）：

- 首行 `# 板块 K 线拉取缓存 2026-09-09 scope=prod codes=4`；次行 `scope=prod codes=BK0428,BK0457,BK0669,BK1173 force_full=False monday_full=False` → **scope=prod 且 4 码 ✅**
- `== 东财请求数=4（scope=prod；改造前 full 口径每轮 65）==` → **≤4 ✅**（非 65）
- beg：`20260909`（BK0428/BK0457/BK1173）/ `20260908`（BK0669），四码 `mode=incremental`、`fallback_full=0` → **1 天增量窗口 ✅**，无 12 年全量（非 20150105/20150101）
- 结果：`== 汇总：ok=1 skip=0 fail=3 fallback_full=0 / 4 ==`、`FAIL 码: BK0428,BK0669,BK1173`、`RemoteDisconnected` → 断言满足但通道仍封。

### 3. unittest —— ✅ 全绿

`.\.venv\Scripts\python.exe -X utf8 -m unittest tests.test_market_context` 末行实况：

```
----------------------------------------------------------------------
Ran 20 tests in 7.256s
OK
EXITCODE=0
```

→ **20/20**。

### 4. 旧实施 job 残留复核 —— ✅ 无残留

`cron action=list` 实机 6 条：`69b20a70`（22:30 Shadow）、`fa6d9cdd`（16:00 K线）、`243d9df4`（本验收 job，running）、`93439da7`（09:30 晨检）、`147825cf`（09-26 季度重估）、`1d99f4e7`（09-10 P1 开工）。**不存在**以「QuantV1 · V3 数据层 P0-1/2/3/4 实施」开头的 job → 旧实施 job `10118c3d` 已随其运行被运行时自动删除，cron list 复核确认无残留，**未执行任何 remove**。

### 5. 总判定

- **验收未全绿**：第 1 项失败（BK0428 / BK0669 / BK1173 三码 `last` 未达 2026-09-09、bars 未 +1）；第 2 项 ✅、第 3 项 ✅、残留复核 ✅。
- 按任务规则：**STATUS 行保持 `PENDING_PULL` 不动**，未翻 COMPLETE（与第 1 实例判定一致）。
- 归因（非改造缺陷）：增量链路已端到端跑通一次（BK0457 16:07：beg=20260909 → 2840→2841、first/covers_oos 不变、二键补齐），另三码 09-09 两班（16:08 / 21:30）仍 `RemoteDisconnected` → **东财 IP 级封禁未解除**；三码待通道恢复由既有 16:00 / 21:30 任务自然补齐（BK0669 将以 beg=20260908 一次补两根），届时需**人工**再验并翻 COMPLETE。
- 待 Summer 处置（无新增缺陷）：① stale 亮灯已成事实（as_of=09-10 期望末根 09-09，三码不满足 → 明日记 `stale_themes`、`usable_for_oos=false`，属 159e4b9 预期防线）；② jobs.py `+60s grace` 补丁仍未重启生效，本次双实例即其后果，建议 11:35–14:50 或今日 23:05 后重启 gateway；③ 21:30 晚间轮表头缺「执行方式=」行待核；④ Phase A 改动仍在工作树未提交，是否入库由 Summer 定。

---

## 运行 2026-09-09 22:45 P0 收尾验收（同 job 第 3 实例，实际 23:01 触发；全只读、零拉取）

- **实例身份**：仍为验收 job `243d9df4`（at 2026-09-09T22:45+08:00）的**第 3 个实例**（前两个 22:45:47 / 22:54:46 已各写一节，见上两节）。同一天 22:45 之后已见三连发，形态与「10:5x 补记 §3」预告的未重启竞态一致 → `jobs.py` 的 `+60s outer grace` 补丁**仍未随 gateway 重启生效**。本节独立取证，不采信也不覆盖前两实例结论。
- **过期守卫**：`Get-Date` = **2026-09-09 23:01:27 +08:00（星期三）** → 日期正确、未过期，继续验收。
- **幂等检查**：R 末行 STATUS 为 `PENDING_PULL`、全文**无** `STATUS: COMPLETE` → 执行验收（不跳过）。
- **边界遵守**：本轮**东财请求数 = 0**（未执行任何 `pull_sector_klines*.py`、未发起任何网络拉取）；`cron` 仅 `action=list` 只读，未新建/删除/触发任何 job；`C:\Users\Turn-\.zcode\v2\tasks-index.sqlite` 未读写；`config.json` 与冻结指标未触碰；Windows 计划任务未重注册；对 R 仅**追加**，STATUS 行未改。

### 1. 四码缓存硬检查 —— ❌ **未过**（4 码仅 BK0457 达标）

取数：`.\.venv\Scripts\python.exe -X utf8` 只读直读 `data/sector_klines/<code>.json`，`bars` 与 `len(klines)` 及首/末根日期逐位比对。

| 码 | bars 实况（期望） | first 实况（期望） | last 实况（期望） | covers_oos | pull_mode | history_verified_at | 文件 mtime | 判定 |
|---|---|---|---|---|---|---|---|---|
| BK0457 | **2841**（2841）✅ | 2015-01-05 ✅ | **2026-09-09** ✅ | True ✅ | `incremental`（`pull_beg=20260909`） | 2026-09-09 16:07:56 | 09-09 16:07:56 | ✅ 全项达标 |
| BK0428 | **2840**（2841）❌ | 2015-01-05 ✅ | **2026-09-08**（09-09）❌ | True | 键不存在 | 键不存在 | 09-08 19:19:14 | ❌ last 非当日、bars 未 +1 → **待人工确认** |
| BK0669 | **2839**（2841）❌ | 2015-01-05 ✅ | **2026-09-07**（09-09）❌ | True | 键不存在 | 键不存在 | 09-07 16:01:22 | ❌ 缺 09-08、09-09 两根 → **待人工确认** |
| BK1173 | **583**（584）❌ | 2024-04-17 ✅ | **2026-09-08**（09-09）❌ | False（任务书明示属正常，**不计失败**） | 键不存在 | 键不存在 | 09-08 16:16:25 | ❌ last 非当日、bars 未 +1 → **待人工确认** |

- **失败点（本项）**：BK0428 / BK0669 / BK1173 三码 `last` 未达 2026-09-09，bars 未 +1（实况 2840 / 2839 / 583，期望 2841 / 2841 / 584）。
- 完好性旁证：4/4 `bars == len(klines)`，首末根与 `first`/`last` 字段一致，**无半成品/错位缓存**。
- 证据项实况：`pull_mode` / `history_verified_at` 仅 BK0457 具备，另三码两键均不存在 → 与第 3 轮 §2 推论（只有成功拉取才写此二键）一致，**反向确认 P0-2 逻辑正确**，非改造缺陷。
- 与第 1、2 实例数值**逐项吻合**（2840 / 2839 / 583、mtime 未变）：三码 22:54→23:01 之间零写入，封禁状态无变化。

### 2. 当日拉取报告 payload 检查 —— ✅ 通过（但结果本身仍是部分失败）

`output/pull_sector_klines_20260909.md`（16:00 OpenSquilla job `fa6d9cdd` 产出）：

- 首行 `# 板块 K 线拉取缓存 2026-09-09 scope=prod codes=4`；次行 `scope=prod codes=BK0428,BK0457,BK0669,BK1173 force_full=False monday_full=False` → **scope=prod 且 4 码 ✅**
- `== 东财请求数=4（scope=prod；改造前 full 口径每轮 65）==` → **每轮请求数 ≤4 ✅**（非 65）
- beg 实况：`20260909`（BK0428/BK0457/BK1173）/ `20260908`（BK0669），`mode=incremental`、`fallback_full=0` → **1 天增量窗口 ✅**，非 12 年全量（无 20150105）
- 结果行：`== 汇总：ok=1 skip=0 fail=3 fallback_full=0 / 4 ==`、`FAIL 码: BK0428,BK0669,BK1173`、错误类型 `RemoteDisconnected`。
- 晚间补拉 `output/pull_sector_klines_evening_20260909.md`（21:30 Windows `QuantFund_KlineEvening`）：同 4 码、`东财请求数=3`、`ok=0 skip=1 fail=3`（BK0457 走 `skip(当日已拉)`），另三码仍 `RemoteDisconnected` → **payload 断言在两班均成立，失败纯因通道仍封**。

### 3. unittest —— ✅ 全绿（20/20）

`.\.venv\Scripts\python.exe -X utf8 -m unittest tests.test_market_context` 输出末段实况：

```
....................
----------------------------------------------------------------------
Ran 20 tests in 7.718s
OK
```

→ **20/20 全绿**。（注：PowerShell 因 unittest 的进度点走 stderr 而把该命令整体标为 `NativeCommandError`/非零退出，属 shell 噪声，unittest 自身判定为 `OK`，与前两实例一致。）

### 4. 旧实施 job 残留复核 —— ✅ 无残留

`cron action=list` 实机 6 条：`69b20a70`（22:30 Shadow）、`fa6d9cdd`（16:00 K线）、`243d9df4`（本验收 job，running）、`93439da7`（09:30 晨检）、`147825cf`（09-26 季度重估）、`1d99f4e7`（09-10 P1 开工）。**不存在**以「QuantV1 · V3 数据层 P0-1/2/3/4 实施」开头的 job → 旧实施 job `10118c3d` 已随其运行被运行时自动删除，**cron list 复核确认无残留，本轮未执行任何 remove**。

### 5. 总判定

- **验收未全绿 → 不翻 COMPLETE**：第 1 项 ❌（三码 `last` 未达 09-09、bars 未 +1）；第 2 项 ✅、第 3 项 ✅、残留复核 ✅。
- 按任务规则：**STATUS 行保持 `PENDING_PULL` 不动**。
- 归因（非改造缺陷）：增量链路已端到端跑通一次（BK0457 16:07：beg=20260909 → 2840→2841、first/covers_oos 不变、`pull_mode`/`history_verified_at` 二键补齐），另三码 09-09 三班（16:08 / 21:30，至 23:01 复核仍无变化）均 `RemoteDisconnected` → **东财 IP 级封禁未解除**。三码待通道恢复由既有 16:00 / 21:30 任务自然补齐（BK0669 将以 beg=20260908 一次补两根），届时需**人工**再验并翻 COMPLETE。
- 与前两实例判定**完全一致**，本轮为第三次独立复核，未新增缺陷。
- 待 Summer 处置（沿用前两实例清单，无新增）：① 明日 `stale_themes` 亮灯属预期防线（as_of=09-10 期望末根 09-09，三码不满足 → `usable_for_oos=false`）；② `jobs.py` `+60s grace` 补丁仍未重启生效 —— 本 job 已在 22:45 之后连发 3 个实例（22:45:47 / 22:54:46 / 23:01:27），建议尽快择窗重启 gateway；③ 21:30 晚间轮表头缺「执行方式=」行待核；④ Phase A 改动仍在工作树未提交，是否入库由 Summer 定。

## 人工复验并收口 2026-09-10 23:2x +08:00（只读复核 + 状态翻转，零拉取）

### 1. 为何现在可以翻 COMPLETE

- 09-09 窗口未过（BK0428 / BK0669 / BK1173 三码 `last` 未达当日），归因为东财 IP 级封禁，**非改造缺陷**；验收 job `243d9df4` 为 at 型、触发后已消失（2026-09-10 复核 `cron` 列表不含该 id），不再兜底 → 按第 3 轮 §3.2 约定，只能**人工再验后翻转**。
- 2026-09-10 16:00「板块K线」在真实通道上取得**全绿**：`== 汇总：ok=4 skip=0 fail=0 fallback_full=0 / 4 ==`、`== 东财请求数=4（scope=prod；改造前 full 口径每轮 65）==`、`FAIL 码: 无`。

### 2. 四码缓存复验（只读实读，2026-09-10）

| 码 | bars | first | last | covers_oos | pull_mode | history_verified_at |
|---|---:|---|---|---|---|---|
| BK0428 电力 | 2842 | 2015-01-05 | 2026-09-10 | True | incremental | 2026-09-10 16:02:45 |
| BK0457 电网设备 | 2842 | 2015-01-05 | 2026-09-10 | True | incremental | 2026-09-10 16:02:48 |
| BK0669 生态农业 | 2842 | 2015-01-05 | 2026-09-10 | True | incremental | 2026-09-10 16:02:50 |
| BK1173 锂矿概念 | 585 | 2024-04-17 | 2026-09-10 | False（预期：仅通道健康探针） | incremental | 2026-09-10 16:02:52 |

- 三码缺口已补齐：BK0669 **2839 → 2842**（一次补 09-08 / 09-09 / 09-10 三根，与 09-09 报告「通道恢复后将以 beg=20260908 一次补齐」的预测一致）；BK0428 2840 → 2842；BK1173 583 → 585。
- `pull_mode` / `history_verified_at` 二键**四码全部补齐** → 与第 3 轮 §2 推论（只有成功拉取才写此二键）完全吻合，P0-2 逻辑再获正向确认。

### 3. 验收四项复核

| # | 项 | 09-09 判定 | 2026-09-10 复核 |
|---|---|---|---|
| 1 | 四码 `last==当日` / bars +1 | ❌（封禁） | ✅ 四码 `last=2026-09-10`，bars 2842 / 2842 / 2842 / 585 |
| 2 | payload 口径（scope=prod · 请求≤4 · 1 天 beg 窗口） | ✅ | ✅ `output/pull_sector_klines_20260910.md`：`scope=prod codes=4`、`东财请求数=4`、beg=20260909 / 20260910 / 20260908 / 20260909、四码 `mode=incremental`、`fallback_full=0` |
| 3 | unittest 20/20 | ✅ | ✅ `Ran 20 tests in 7.002s / OK`（离线单测，未触东财） |
| 4 | 旧实施 job 残留复核 | ✅ | ✅ `cron` 复核 6 条，无 `10118c3d`、无 `243d9df4` |

### 4. 边界遵守（本轮）

**东财请求数 = 0**（未执行任何 `pull_sector_klines*.py`，未发起任何网络拉取）；未新建 / 删除 / 触发任何 cron job；`C:\Users\Turn-\.zcode\v2\tasks-index.sqlite` 未读写；`config.json` 与冻结指标未触碰；Windows 计划任务未重注册；对 R 仅**追加本节 + 翻转末行 STATUS**，前文一字未改。

### 5. 收口说明

- 09-09 那一班**确实未达标**，本节不追认其为绿：按「如实保留失败窗口 + 凭次日全绿复验收口」的方式翻转，保留可回看性。
- Phase A（`pull_sector_klines.py` / `pull_sector_klines_evening.py` / `pull_sector_klines_burst.py` / `deploy/install_scheduled_tasks.ps1` / `README.md`）已与本节**同批提交入库**，第 1/2/3 实例 §5.④ 的遗留项就此关闭。
- 本 R 的 at 兜底与自动翻转已不可能发生，故本次翻转由**人工**执行，符合报告既定约定。

STATUS: COMPLETE
