# 历史 14:55 快照可回补性只读探测方案（2026-09-23 起草，未执行）

> 状态：**方案，零执行**。任何涉及外部请求的步骤（G1/G2/G3）须经 Summer 显式授权并过铁律 7 四项闸门后方可运行。
> 触发背景：P0-1（抓取范围 65→4 码）已实施，但它缩的是日线板块抓取面；**14:55 时点快照缺口与它正交，依然存在**。

## 0. 缺口定义（本节全部已核实，本地证据）

- `data/intraday/` 现存 22 个文件（2026-08-25 → 2026-09-23）**全部为 `{date}_mid.json`（11:30 槽位），无任何 `{date}_post.json`**。
- 代码根因：`run.py:465-466` 仅 `slot=="mid"` 时调 `save_snapshot`；post 槽位从未以此形式落盘。且 `core/intraday_store.py:27-29` 只保留最近 30 个文件——即使今后补写 post 也会滚动淘汰。
- 部分补偿：`data/intraday_features.jsonl`（08-31 起）已有 slot=post 的模型协议特征行，但受 MODEL_VERSION 门控、仅 4 基金、非特征全集 → 不能替代快照文件。
- 影响面（既有文档自陈）：`backtest_action.py:8`、`backtest_fusion.py:8` 明确「免费接口无历史 11:30/14:55 快照 → 以日线收盘近似 14:55（差约 5 分钟）」「11:30 基线无法历史回测」。

## 1. 回补目标的精确刻画

14:55 快照字段（对照 `data/intraday/2026-09-23_mid.json` 与 `core/intraday_features.py:43`）全部由「季报前十大持仓权重 × 个股实时涨跌」合成：

- `est_return` = Σ(权重 × 个股截至 14:55 涨跌幅)
- `breadth` / `n_up` / `n_down` = 个股 14:55 涨跌方向按权重聚合
- `concentration` = max|贡献|/Σ|贡献|；`covered_pct` / `holdings_age_days` / `reliability` 可由持仓披露日期离线重建

**⇒ 回补唯一外部数据依赖 = 每只重仓股在历史交易日 D「截至 14:55 的涨跌幅」**，即分钟级 K 线（或分时）。不需要基金本身的分钟线。

- 标的池：4 基金（002112/002207/022853/025687）× PIT 季报前十大并集（covered_pct 70~82% → 每期约 35~40 只去重），随季报切换。
- 同一数据源可顺带回补 **11:30 槽位**（同一日的 11:30 bar）→ 有望一并解开「11:30 无法历史回测」。

## 2. 探测问题清单（每条都要有可判定答案）

| # | 问题 | 判定物 |
|---|---|---|
| Q1 | 东财 push2his `kline/get` 分钟线（klt=5 / klt=1）是否可用？历史深度各多少？ | 最早可得 bar 日期 |
| Q2 | 腾讯 ifzq 分钟线（mkline/m5，**仓库零先例，端点拼写需实测**）可用性 + 深度？ | 同上 |
| Q3 | 14:55 bar 语义：bar 时间戳标注是 14:55-14:59 还是 14:50-14:54？ | 时间戳样本 → 固定回补口径 |
| Q4 | 分钟线 fqt 复权行为；除权日「截至 14:55 涨跌幅」与前收口径如何取 | 除权样本前后一致性 |
| Q5 | 双源一致性：同日同股 14:55 close 东财 vs 腾讯差异（bps 级）？ | 差异表 → 决定可否单源采信 |
| Q6 | 真 14:55 涨跌幅 vs 日线收盘近似（现行口径）误差多大？ | 直接量化 backtest_action/fusion 近似噪声 |
| Q7 | 全量回补请求量（标的池 × 天数 ÷ 单请求 bar 数）与频控风险评级 | 对照 09-08 频控事故 |

## 3. 探测步骤（全部只读；产物只写 `output/`，不碰 `data/`、不改任何 `.py`）

### G0 本地零网络取证（无网络请求，可先行，无需授权）
1. 从 `basket_20260902.json` + 持仓历史枚举 PIT 标的池与日期范围（以 backtest_action / backtest_delta_intraday / backtest_fusion 的消费跨度为准）。
2. 审计 `intraday_features.jsonl` 现有 post 行覆盖（哪些日已有、缺哪些字段）。
3. 产出《回补目标清单》：date × stock 矩阵规模、单日请求数估算（供 Q7）。

### G1 东财单码探针（须授权 + 四闸门）
单只流动性个股（如 sh600519），原生 urllib + http 主链路（09-10 已验证的畅通姿势），请求预算 ≤12 发、间隔 ≥2s：
- klt=5：单日样本（如 2026-09-22）→ 逐级回探 beg（2026-06 → 2025-09 → 2024-09 → 2022）记录最早可得 bar；
- klt=1：只探深度（预期很短）；
- fqt=0/1 对照一根 bar；如样本恰逢除权日顺带核对 Q4。

### G2 腾讯单码探针（同上授权）
- ifzq m5 端点先 1 发验证返回形状（拼写未核实），再探深度，预算 ≤6 发。
- 已知坑备查：`fqkline` 对 BK 板块码返回空（09-08 实测）；本次标的为个股/ETF，预计不受影响，仍以实测为准。

### G3 交叉验证（复用 G1/G2 返回，新增请求 ≤4 发）
- 双源同日 14:55 close 对照（Q5）；各自与日线 close 对照（Q6）。

### G4 判定报告
- 产物：`output/ops_runs/<执行日>-probe-intraday-backfill-result.md` + 同名 json（source × klt × 深度 × 一致性 × 请求量 × 风险评级）。
- 结论三态：可回补 / 部分（仅近期窗口）/ 不可；若可行，附回补实施立项建议（**回补脚本本身不在本方案范围**，实施须另走预注册 + 指纹不漂移核对，对齐 09-10 前复权重拉教训与 P0-2 前缀断言纪律）。

## 4. 纪律边界（对齐 QuantV1 AGENTS.md 铁律 7/8）

- G1/G2/G3 涉及外部请求 → **执行前必须 Summer 显式授权**，过四项闸门（上游任务已完成 / 无频控征兆 / 无并行拉取 / 时间盒内）。建议窗口：交易日 21:30 晚间补拉完成之后，或周末白天。
- 任一源首发即 `ERR_EMPTY_RESPONSE` / 非 200 → 该源立即停手记录，不重试、不换姿势、不跑 pw 兜底（09-08 十二次重启全灭的教训）。
- G0 零网络，不受上述约束。
- 失败平铺直叙（铁律 5）；探测全程不进入任何生产数据路径。

## 5. 已核实 vs 未核实（诚实口径）

**已核实**：缺口形态（data/intraday 仅 mid）；run.py 只存 mid；30 文件滚动淘汰；日线近似 14:55 的既有回测约定；腾讯对 BK 码无覆盖；东财频控史（09-08 五轮全灭、09-10 自愈）与畅通姿势（http + 原生 urllib）。
**未核实（正是探测要回答的）**：东财/腾讯分钟线端点可用性与历史深度、14:55 bar 时间戳语义、除权行为、双源一致性、全量请求量。
