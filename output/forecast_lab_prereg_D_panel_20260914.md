# 预注册 · 候选 D：研究池扩容面板（买功效）——规格冻结（2026-09-14）

> **状态：🟢 已拍板（2026-09-14 09:3x，见 §九 + 修订节 R2）/ 未实现。** 本文先于任何实现落盘（铁律：判据先写死，跑前不改；要改 = 另起修订节）。
> 拍板依据：Summer 2026-09-14 00:12「先给 D」（09-16~18 窗口任务序：D 前 C 后）。
> 上游证据：`output/forecast_lab_mde_20260912.md` §二/§三/§四（MDE80=0.117、宽度≈2、判据分叉）；
> `output/forecast_lab_prereg_rules_20260913.md` §一 R1（D1-c 双端点双门槛，本注册全部继承）。

---

## 〇、D 是什么、不是什么

- **是**：把评估面板从「4 只场外基金（有效宽度 2~4）」扩到「4 基金 + 13 主题 ETF/板块代理」的研究池，
  为**训练侧与选择侧**买统计功效；评估侧仍以生产 4 只为最终留出。
- **不是**：不改变晋升门。D1-c 的 G1（绝对）/G2（相对）判定口径逐字不变，且**只认生产 4 只留出子集**；
  研究池 pooled 结果是第三层参照，**不得替代生产门**（estimand 变更诚实声明）。
- **已知极限**：D 修不了生产留出端本身的宽度封顶（2024-12 前只有 2 只基金在场）；
  它买的是「选出来的候选更可能对」+「研究池端点的可判性」，生产端 CI 半宽仍 ~0.06 量级。

## 一、面板成员（主代理；relaxed 分层纪律继承 basket 原注）

| 层 | 成员 | 本地缓存首根（09-13 实测） | 闸门 |
|---|---|---|---|
| 基金（评估留出 = 全部 4 只） | 002112 / 002207 / 022853 / 025687 | 2015-11-16 / 2015-12-02 / **2024-12-24** / **2025-10-14** | D-lite 统一自 **2020-04-27** 起 |
| 原闸门代理 ×9 | 512480 半导体, 512880 证券, 159915 创业板, 512660 军工, 510880 红利, 512800 银行, 160225 新能源, 512010 创新药, 501030 公用事业 | 2013-07 ~ 2019-06 | 通过 |
| relaxed 代理 ×3 | 159611 电力(22-01), 515220 煤炭(20-03), 159825 农业(20-12) | 见左 | **单标记分层报告，不混池** |
| 板块指数 ×1 | BK0457 电网设备（primary；159326 仅 fallback 启用时进 relaxed 层） | 2015-01-05 | 天然通过 |

- 全部 13 主代理**已在本地缓存**（`data/stock_klines/` + `data/sector_klines/`，mtime 09-11）→ 冻结仅需一次受控批量刷新 + 同批双值指纹，**无新增数据采购**。
- 面板规模粗估 ~2×10⁴ 行（现有 3371 行的 6~8×；以冻结实测为准，此数不进判据）。

## 二、特征空间（复用存量，零新写）

- `experiments/forecast_lab/features_a158lite.py`：50 个纯价格结构特征（10 族 × 5 窗），
  量纲无关 → **NAV 与 ETF 价格直接同尺可比**；自带因果性/NaN/负下标自检（256 测试包在册）。
- PIT 口径原样继承：样本日 d 只用 `series[:i]`（截至 T-1）；短窗历史不足 → NaN 不缩窗。
- 缺失掩码协议继承 B1：50 特征 × (值,mask) = **100 维**，训练/推理同一函数。
- CORR 为「收益率滞后 1 阶自相关」替代（净值/价格无成交量），报告中显式标注（模块原注）。
- 代理价格取 `klines` 行的 **close（下标 2）**，前复权 fqt=1；基金取 `navs` 单位净值。

## 三、标签（双端点，继承 D1-c）

- `fwd5` 绝对收益：`series[t+5]/series[t] − 1`（末日 = 缓存末根回退 5 个交易日，标签完整才入面板）。
- `rel5` 相对收益：同日**在场成员**横截面超额（均值/中位两把尺子照旧，all arms 共用）。
- 单位一致性论证（D-lite 范围内）：002112 全部 5 次分红除息在 2018-06-26~2019-07-17，
  另 3 只分红为 0 → **2020-04-27 起基金单位净值收益 ≈ 含权收益**，与 ETF 前复权（含权）同语义。
  残余口径差（ETF 分配再投时点 vs 基金）记为已知近似，不进本窗口判据。

## 四、PR-20260911-01（分红除息）触发分析 —— 本窗口**不触发**，依据写死

- 触发条件 = 「样本区间触及 2018-06-26 ~ 2019-07-17 的**单位净值日收益**」。
- D-lite 基金侧起点 2020-04-27 → 污染窗口内**无任何基金行**；代理行为前复权含权序列，与派现无关。
- ∴ 本注册不实施 PR-01，登记表维持 🟡；**任何把基金历史前延到 2019-07-17 之前的实验（D-full）必须先三选一**
  （预设推荐：方案 1 改用复权净值——`lsjz.acc_nav` 已在本地缓存，⚠️ 2015 段覆盖率未核验，属 D-full 前置检查项）。

## 五、冻结与指纹（双值纪律强制产出）

1. 09-16 批量刷新 17 序列 + 4 基金净值（受控一次，预算内；此后一切读数零网络）。
2. 面板生成器与 K 线指纹**同批产出**（09-12 实证漂移 186/240 条 → 冻结不带指纹 = 无效冻结）。
3. ⚠️ 实施检查项：`kline_fingerprint` 现覆盖 240 股 + 4 基金——**须核验 13 个面板代理是否在其遍历集**；
   不足则扩到面板成员全集（只扩清单，不改指纹算法）。
4. 降级/频控征兆 → 立即停手不重试（继承 `freeze_samples.py` RATE_MARKERS 纪律，退出码 1）。

## 六、D 自身判据（三条全硬，任一不过 = D 失败，只报告不放宽）

- **T1 可复现**：面板 JSONL（canonical sort_keys）同批输入两次生成 sha256 逐字节一致 + 指纹一致。
- **T2 单位对齐**：面板基金行与 `samples_frozen_20260910.jsonl` 行身份及共享列（7 特征 + fwd* 标签）
  逐值一致（容差 1e-9，NAV 历史应只追加不改写；不一致 → 停，查明再报）。
- **T3 功效达标**：冻结后跑 M0 式功效仿真（面板宽度、日块 bootstrap、B=1000、seed=42），
  实测**面板配对 MDE80 ≤ 0.05** 且单模型日 IC 重抽样 sd 较 0.0307 显著下降 → 才开候选槽；
  达标前不得跑任何候选结论（防「尺子没修先量东西」）。

## 七、候选槽继承判据（T3 达标后）

- 任何候选在面板上跑 = 唯一变量法（继承 A 的纪律），G2 四条**逐字沿用** prereg_rules §一 R1：
  配对 pct2.5(D)>0、逐折不劣 ≥3/4（面板折按 `build_wf_folds` 原参重建，OOS start 2025-04-30 不动）、
  跨尺子稳健含 CI、用途锁定条款原样携带。
- **晋升判定只看生产 4 只留出子集**；面板 pooled 与生产子集永远两层并列报告，禁止择层。
- 折构造变更（面板行集不同 → 折边界不同）预注册在案：base 臂须先在面板上重建基线，
  候选只对同折 base 做配对，不与旧 4 只池数字直接横比。

## 八、时间与预算（09-16 ~ 09-18，合计 ~11h）

- 09-16：刷新+冻结+指纹+T1/T2（~4h）
- 09-17：T3 功效表 + 面板 base 臂（~3h）
- 09-18：**首个候选槽**（待拍板：A″ 面板复证 或 C′ regime_cond；建议 A″——现成协议、
  直接检验「新尺子下 A 的 G2 余量是否扩大」，C′ 随后）+ 两层报告（~4h）
- D-full（基金史前延 + PR-01 实施）不占本窗口，挂 09-26 季度重估节点。

## 九、Summer 勾选结果（2026-09-14 09:3x 落定；原推荐项未被采纳处已在 R2 注明）

- [x] **范围**：**D-full**（非推荐项 D-lite）→ 但时间归属按 §八 原文：**不占本窗口，挂 09-26 季度重估节点**（见 R2 之一）。
  ∴ **本窗口 09-16~18 实际跑 D-lite 面板**（基金史自 2020-04-27），PR-01 在本窗口内不触发。
- [x] **09-18 候选槽**：**C′ regime_cond**（非推荐项 A″）。
  ∴ R1 关于 A″ 的特征空间澄清在本窗口**不生效**，保留作后续启用 A″ 时的依据。
- [x] **T3 失败路径**：**不采用**原「失败即停、不临场放宽」条款；改为**事前预注册失败路径**（R2 之二）。
  —— 该改动于**跑之前**写死，不构成临场放宽，判据阈值本身未放松。

## 十、明确不做

- ❌ 不改 config / model_registry / registry.json / 生产 `.py` / 定时任务（面板件只进 `forecast_outputs/`）。
- ❌ 不动 D1-c 门槛与 shadow 降级授权（两件事解耦；10-12 到期复核仍走 PR-20260913-01）。
- ❌ relaxed 代理不并入原闸门池出单一数（分层报告，basket 原纪律）。
- ❌ 不重跑晋升（derive_promotion v1 与 verify_approval 行为不变）。

## 十一、修订记录（追加，不覆盖原文；判据阈值未动）

**R1 · 2026-09-14（落盘当日、勾选开跑前）——A″ 特征空间澄清（estimand 边界，非判据变更）**
- 源码已核实（`core/intraday_features.py::compute_features`）：现 7 特征全部依赖 holdings+quotes，
  **proxy 无季报持仓可穿透 → 7 特征在面板上不可算**。
- ∴ §八/§七 的「A″ 面板复证」明确为：两臂（训练=绝对 fwd5 / 相对 rel5）**统一用 §二 的 a158 特征空间**。
  面板内仍是「标签 = 唯一变量」；但与旧 A（7 特征、4 只池）**不是同一模型**——
  A″ 是「新尺子上的标签实验」，不是旧 A 的严格复证；与旧数字只做方向参照，不做跨池配对（§七 已禁横比）。
- 生产 4 只留出端评估同样取 a158 特征（NAV 可算、与面板同尺），保证「面板选择 → 生产验证」特征口径一致。
- 本节不改变 §六 T1~T3 与 §七 G2 四条的任何阈值。
- 〔2026-09-14 09:3x 注〕Summer 选定候选槽为 **C′**，∴ 本窗口不启用 A″；本节保留作后续启用时的依据。

**R2 · 2026-09-14 09:3x（Summer 勾选落定、开跑前）——三项拍板 + 事前失败路径 + D-full 归属**

### R2 之一 · D-full 的时间归属与 PR-01 实施方案 1

- Summer 选 **D-full**，同时确认 §八 口径「D-full 不占本窗口，挂 09-26」。
  → 与 §九 原文「D-full＝本窗口 +1 天起」的自相矛盾**以 §八 为准**；§九 已按此改写。
- **PR-20260911-01 实施方案选定：方案 1（改用复权净值）**。⚠️ 但本条有两个**开跑前必须验证的前置**，
  登记于 PR-01 之下，任一不成立 → 走 R2 之三的事前失败路径，**不得临场改选方案**：
  - **前置 G1（复权源可得性）**：登记表 §已知约束 L53 早已写死——同花顺 `performance/nav` 的 `range`
    上限 `fyear`（实测最早 2021-09-10）→ **拿不到 2018-2019 复权净值**。故方案 1 只能走：
    (a) 东财 `lsjz` 分页抓 `acc_nav` 自算，或 (b) 用 PR-01 §事实底数的**5 次派现表自算复权序列**。
  - **前置 G2（本地缓存实测，本次新发现）**：预注册 §四 原注「`lsjz.acc_nav` 已在本地缓存」**不成立**。
    实测 `data/klines/*.json` 的 `lsjz.records` **每只仅 20 条**（2026-08-14→09-10），
    因 `config.json:120` 的 `lsjz_url` 硬编码 `pageIndex=1&pageSize=20`。
    → 污染窗 2018-06-26~2019-07-17 的 `acc_nav` 本地覆盖 = **0 行**。
  - **∴ 方案 1 的无网络实现 = 上列 (b) 派现表自算**（5 个除息点、全序列一次性重算含权净值）。
    (a) 分页抓取需 ~132 请求/基金，撞东财 IP 级频控（09-08 实证可连封 5.5h+），
    **禁止在 09-26 窗口之外的任何交易日执行**；若 (b) 足以覆盖，则 (a) 不做。
- **D-full 增量实测**（供 09-26 评估，不进本窗口判据）：2020-04-27 之前的基金行 = 002112 **1089**
  + 002207 **1077** = **2166 行**（022853 / 025687 均为 0，成立过晚），占面板粗估 ~2.0e4 行的 **8~11%**；
  其中落在污染窗内 **262 × 2 行**。

### R2 之二 · T3 事前预注册失败路径（替代「只报告不放宽」的停摆后果）

Summer 拍板不采用「不过即整轮作废」，改为**跑前写死失败路径**。判据阈值 **0.05 不动、不放宽**；「不过时走哪条」预先规定如下：

- **T3 通过** → 按 §七 开候选槽（C′ regime_cond）。
- **T3 不过（首次）** → ① 立即停候选槽（**不得**在未达标尺子上出任何 G2 结论）；
  ② 按冻结实测报告「面板宽度、实际 MDE80、距 0.05 的缺口」；
  ③ 自动登记 **PR-20260914-02**（面板宽度再扩档 / 或折构造调整）为**事前指定的下一动作**；
  ④ 本窗口剩余预算转做 D-lite 面板的 **T1/T2 复现 + 指纹留档**（不浪费冻结），
    并在报告中显式标注「D-lite 面板 = 未通过功效检验的中间产物，不得用于任何候选裁决」。
- **第二次仍不过** → 停止本窗口一切 D 侧动作，D 整体挂 09-26 季度重估，与 D-full 并案。
- **禁止项**：不得调低 0.05 阈值、不得改 bootstrap B/seed、不得以「趋势向好」为由跳过 T3。
  这些动作只能以**新的预注册文**提出，且须 Summer 单独拍板。

### R2 之三 · 版本可审计性缺口（提请，未擅自动）

- 本文所在 `output/` 被 `.gitignore` 的 `output/*.md` 整类忽略 → **预注册文不在 git 历史内**，
  「判据先写死、跑前不改」目前**无法用提交历史证明**（09-13 的候选 A 预注册文同样未入库）。
- 建议（须 Summer 同意，属 `.gitignore` 变更）：对 `output/forecast_lab_prereg_*.md` 加例外规则
  （`!output/forecast_lab_prereg_*.md`）并 `git add -f` 收编历史版。**本窗口未执行**，仅登记。

—— 预注册 · 2026-09-14 · 纸面研究，不构成投资建议
