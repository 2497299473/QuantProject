# 预注册 · 草案 D2b：门槛②重抽样校准修复（方向乙「修尺子」）——待拍板

> **状态：🟢 已拍板（2026-09-16 13:40，Summer「按你的建议执行」）/ 执行中——探针 v0/v1/v2 已挂机跑。**
> 登记依据：`deploy/preregistered_items.md` PR-20260914-02 §触发时必须做的动作 #1。
> 上游：`output/ops_runs/2026-09-16-t3-panel-power.md`（T3 FAIL 实录）、候选 D 预注册 §六 T3 + R2 之二。
> 纸面研究，不构成投资建议。

## 〇、要解决的问题（事实底数，2026-09-16 实测）

1. 门槛②（配对差值日块 bootstrap pct2.5 > 0）的 I 类错误率在面板结构下从**旧池 0/50**
   （923 行）变为**面板 6/50**（4428 行 / 332 日块 / 日宽 13–14）→ 按 M0 协议判「尺子失真、MDE 作废」。
2. 二项检验：若真误报率 = 0.05，P(≥6/50) = 0.038（临界）；6/50 的 95% CI = [0.045, 0.243]。
   → **0.12 这个读数本身分辨率不足**：可能真失真，也可能是名义 α 下的小概率抖动。
3. 后果链：尺子不修，任何宽度扩档（方向甲）跑出的 MDE 都不可解释 → 本草案与 D2a 的顺序是
   「**乙先行或并行，最终裁决一律用修好的尺子**」。

## 一、estimand / 推断手段的分界（本草案的立身前提，写死）

- **estimand（不许动）**：被检验的对象 = 「WF(63 日窗, LGB a158+mask) 训出的候选 vs 基线，
  在面板 OOS 段（oos_start 2025-04-30 起）的 T+5 日横截面 RankIC 之差 > 0」。
  其中 **window_days=63、折构造、OOS start 属于 estimand 定义**——改它们 = 换被检验的对象，
  本草案**明令不做**（如将来需要，另起草案单独拍板）。
- **推断手段（可修）**：用 bootstrap 从样本重构成差值的抽样分布，是估计 CI 的**手段**。
  门槛②原文「配对差值 bootstrap CI 下界 > 0」没有写死重抽单位必须逐日独立——
  生产 `cluster_bootstrap_ci` 的日块实现是「同构参照」而非 estimand 本身。
  本草案只在**手段层**动刀。
- ⚠️ **波及声明（如实）**：若修复结论要求变更重抽样方案，生产门槛②的实现
  （`cluster_bootstrap_ci` 同构）将与之**失同步**——这属于判据实现变更，必须另提请 Summer
  拍板并改 D1-c 文档，本草案阶段**不动生产任何东西**，只在研究层（forecast_lab）实验。

## 二、探针矩阵（全部零网络，`run_t3_panel_power.py` 加 variant 参数实现）

| 代号 | 手段变更 | 假设 |
|---|---|---|
| **v0** | 现状复刻（i.i.d. 日块重抽，B=1000），仅 N_PERM 50→200 | 钉死 0.12 是否真实（SE 0.052→0.023） |
| **v1** | **moving block**：按连续 5 日块为单元重抽（保留短程 IC 自相关） | 面板日块行数变大后，独立日块假设低估了块间相关 → 差值分布偏窄 → 误报升 |
| **v2** | **两阶段**：先按折重抽、折内再抽日块（B 不变） | 折间异方差（逐折 IC [−0.061…+0.186] 翻转）是 pct2.5 失准主因 |

每格统一报告三读数：① 轮级误报率（N_PERM=200）② base-vs-base sanity（判出必 False）
③ ρ=0.10 单点 power（不得较 v0 塌缩 >20%——防止「把尺子修钝到什么都判不出」）。

**选择规则（先写死）**：取满足「轮级误报 ≤0.10 且 ③ 塌缩 ≤20%」的 variant 中**改动最小**者
（优先序 v1 < v2，即若 v1 合格则不采纳 v2）；三个都不合格 → 尺子不可修，本路径宣告失败，
挂 09-26 与 D-full 并案（**不做第三份草案，防无限调尺子**）。

## 三、T3 复跑与终局判据（阈值零改动）

- 选定的 variant 固化进 T3 主流程 → 复跑 §六 T3 全套：
  **判据 A MDE80 ≤ 0.05、判据 B ic_sd < 0.0307、尺子前提轮级误报 ≤ 0.10（N_PERM=200）**；
  B=1000、seed=42、relaxed 不混池、主判据 14 主池——R2 之二禁止项逐字继承。
- 三判据全过 → 候选槽恢复，09-18 按原 §八 排期执行（C′ regime_cond）；
  A 不过（尺子修好了但 MDE 仍 >0.05）→ 恰好证明功效缺口是真的 → 转 D2a（宽度扩档）拿实测说话。

## 四、成本与排期（估算）

- 实现：T3 脚本加 variant 开关 + 自检，约 40 分钟人工。
- 机时：permutation 每 variant ≈ 200 轮 × ~20s ≈ **70 分钟**；三 variant ≈ 3.5h，零网络
  → 可今晚挂机跑（R3 硬线只约束东财请求，不约束离线计算）；T3 复跑 +25 分钟。
- 里程碑：今晚起跑 → 明晨（09-17）12:00 前出探针矩阵 → 09-17 下午出 T3 终局 → 09-18 候选槽判定。

## 五、明确不做

- ❌ 不动 MDE80=0.05 / ic_sd=0.0307 / 容差 1e-9 三个判据点；不动 B=1000 / seed=42。
- ❌ 不动 window_days=63 / 折数 / oos_start=2025-04-30（estimand 层，见 §一）。
- ❌ 不把门槛②换成 t 检验等其它推断族（那是改判据，不是修手段）。
- ❌ 不碰 config / registry / 生产 `.py` / 定时任务；不动 relaxed 分层纪律。
- ❌ 探针不合格时不追加第四变体（§二选择规则已封顶）。

—— 草案 · 2026-09-16 · 定稿前提：Summer 拍板 + 本文件判据段不再修改

---

## 修订记录（追加，不覆盖原文）

**R1 · 2026-09-16 13:40 · Summer「按你的建议执行」→ 判据段冻结，探针已挂机执行**

- **判据/选择规则/封顶条款全部冻结**（§一~§五 不再修改）。
- **实现**：`experiments/forecast_lab/run_d2b_ruler_probe.py`（selftest 13/13 通过；零网络守卫同 T3 口径）；
  存量引擎 `run_m0_power.PairedBoot` 加**向后兼容** `picks=None` 参数（默认路径逐字节不变，已由 selftest 项 1/8 断言）。
- **效率（不改语义，§二 矩阵与判据一字未动）**：base 重训 / MDE 候选构造 / 置换重训 与 variant 无关 → 各只付一次
  并缓存为 (200, 4428) 预测矩阵，仅「判定」按 variant 各算一遍 → 实测机时约 62 分钟（远低于 §四 的 3.5h 估算）。
- **执行记录**：13:42 冒烟（`--variants v0 --n-perm 4`）→ **v0 三重复刻 T3 全部命中**
  （pooled IC +0.0235、判据 B ic_sd 0.0214、MDE80 0.0651、ρ=0.10 power 0.920）；折归属
  {66,66,67,66,67}=332 块、完整覆盖 OOS。→ 13:4x 全量挂机（`--n-perm 200`，三 variant）。
- **待明晨产出**：`forecast_outputs/d2b_ruler_probe_20260916.json`（per-variant 三读数 + 选择规则结论）。

—— 修订 R1 · 2026-09-16 · 判据段冻结，纸面研究，不构成投资建议
